{
  "name": "draft-assistant-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-end harness. Deliberately a separate npm package with its own lockfile: WebdriverIO pulls ~115 MB and a pile of advisories with no fix available, and neither belongs in the app's everyday `npm ci` or its `npm audit` gate.",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "wdio run wdio.conf.ts"
  },
  "devDependencies": {
    "@wdio/cli": "^9.24.0",
    "@wdio/local-runner": "^9.24.0",
    "@wdio/mocha-framework": "^9.24.0",
    "@wdio/spec-reporter": "^9.24.0",
    "@wdio/tauri-service": "^1.3.0",
    "typescript": "~7.0.2"
  }
}
