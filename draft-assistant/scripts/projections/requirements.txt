# Dependencies of the projections pipeline. Nothing here is needed to build
# or run the app itself — only to regenerate the CSV it imports.
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
pandas>=3.0.5
requests>=2.34.2
pypdf>=6.16.2
lxml>=6.1.2
