# `engines` in package.json is advisory unless this is set: without it a
# contributor on Node 20 gets a confusing downstream failure instead of a
# clear "wrong Node version" error from npm ci.
engine-strict=true
