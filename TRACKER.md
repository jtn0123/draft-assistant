# Draft Assistant — grade & tracker (2026-08-30)

## Current focus: stability · performance · lazy load · week-one assistant · status

| #   | Item                                                                    | Status         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ----------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Lazy-load the big UI chunks (Chat, SeasonScreen, DraftScreen)           | done `b4f2c39` | Entry bundle 273 → 228 KB; each screen is its own chunk, Chat only fetched when opened.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2   | Board sort caching without staleness (audit G7)                         | done `c8ca01e` | New `boardIdentity.ts` deep-compares players and reuses the array only when observationally identical — re-sort and ~400 row re-renders skipped on no-op ticks, staleness impossible by construction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 3   | Per-source status + analysis age                                        | done `4cecd63` | Live badge tracks matchups/scores/rosters separately with a plain-language tooltip; trade/waiver ideas show "from N min ago" when the cached analysis is older than 2 min.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 4   | Week-one assistant upgrades (reasons, injury flags, decision deadlines) | done `bb61687` | New `season_calls.rs` (advice) + `season_injury.rs` (Sleeper's dozen injury spellings → Q/D/O). Every call carries a plain-language `reason` and a `locks_at_ms` deadline from the scoreboard's `start_time` ("decide by Sun 1:00 ET"); the head-to-head table tags injured starters on both sides with the word on hover; a starter listed Out/Doubtful raises a call the projections alone would miss, sorted to the top and kept out of the "points on the table" total. All fields additive/optional, so the checked-in fixture still loads. `matchup_rows` moved to `season_view_parts.rs` to keep `season.rs` under the cap.                                                                                                                            |
| 5   | Split `build_season_view` by section (audit A5)                         | done `c144643` | `season_view_parts.rs` dissolved into six section modules — `season_lookup.rs` (the Lookup primitive), `season_view_matchup.rs` (head-to-head + start/sit calls), `season_view_live.rs`, `season_view_standings.rs`, `season_view_market.rs` (waivers + trades) and `season_view_feeds.rs`. `season.rs` drops 480 → 340 lines and keeps only `SeasonView`, `SeasonAnalysis` and the orchestration; no section file exceeds 250 lines or 10 imports. `MatchupSection` hands its working values forward so nothing is recomputed. Audit A2's `matchup_for`/`opponent_of` moved to `season_api.rs` on the way; `why_start` stayed with the calls plumbing rather than going into the pure lineup solver. Golden tests passed unmodified — zero behaviour change. |

Follow-ups found during this work:

- `DraftPollMemory::picks_changed` keys on pick _count_ only — a commissioner replacing a pick without changing the count never reaches the UI (`poll.rs`). done `a1af9e0` — now compares count plus a hash of every pick number and player id.
- Pre-commit hook was broken for git worktrees (validated the main checkout). done `b4f2c39`

---

## Trends/League tab debug (2026-08-31) — `a2cafae`

Six bugs found by driving the running app rather than reading it. Bugs 1-5 are fixed in `a2cafae`; bug 6 (the blank League-tab headshots) landed separately.

| #   | Bug                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Fix                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Pinned by                                                                                                                                                                                                                                    |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Trends legend row: `grid-template-columns` declares 4 columns, the markup rendered 5 children — avatar took the flexible column, the team name truncated to "Northe…" in the 52px value column, and the delta wrapped onto an implicit second row. Only reproducible with ≥3 snapshots _and_ real movement, which the checked-in 2-snapshot fixture never had.                                                                                                                                                                                                                                                              | Picture and name wrapped in one `ellipsis team-cell`, the way `Ranked` already did it. Row height 51px → 29px, one grid row.                                                                                                                                                                                                                                                                                                                                               | `TrendsTab.test.tsx` — "keeps picture and name in one cell so the row has four children"                                                                                                                                                     |
| 2   | 15 `button > button` pairs in the DOM: `TeamAvatar`/`Headshot` always wrap their picture in `Zoomable` (a `<button>`), inside the trends legend row and the season calls row, which are themselves buttons. Invalid HTML with an inner control keyboard and screen-reader users cannot resolve. (Click-through was already prevented; this was validity/a11y only.)                                                                                                                                                                                                                                                         | Opt-in `interactive={false}` on `TeamAvatar`, `Headshot` and `PlayerName` renders the same picture with no button wrapper. Zoom kept everywhere it is standalone.                                                                                                                                                                                                                                                                                                          | `TrendsTab.test.tsx` + `ThisWeek.test.tsx` — `querySelectorAll("button button")` is empty in both panels                                                                                                                                     |
| 3   | Trends fallback copy always blamed the third reading, but `plottable` needs three readings **and** movement — five flat snapshots told the user to wait for a third they already had.                                                                                                                                                                                                                                                                                                                                                                                                                                       | The two cases read differently: "…across 5 readings. No team has moved enough to plot yet."                                                                                                                                                                                                                                                                                                                                                                                | `TrendsTab.test.tsx` — one test per branch                                                                                                                                                                                                   |
| 4   | `public/dev-season-fixture.json` stored the _derived_ `recent_trades[].sides[].gets`, and three of five trades still had `"gets": []` from before `season_deals` learned to name traded picks. The preview and the whole Playwright suite only ever saw the "gets draft picks" fallback. The Rust side was already correct.                                                                                                                                                                                                                                                                                                 | Patched with the labels the current code produces ("2027 2nd", "2026 1st, 2027 3rd"); every other value byte-stable.                                                                                                                                                                                                                                                                                                                                                       | `fixture_shape.rs` — "every trade side in the season fixture names what it got"                                                                                                                                                              |
| 5   | `TrendChange.at` had no unit doc; `TradeDone.at` next door is milliseconds and the UI divides only that one by 1000. Both correct today, but the two call sites read as contradictory.                                                                                                                                                                                                                                                                                                                                                                                                                                      | Doc comment stating seconds and naming the contrast. (`TrendPoint.at` was already documented.)                                                                                                                                                                                                                                                                                                                                                                             | n/a — doc only                                                                                                                                                                                                                               |
| 6   | Mike Evans (and one or two others) had a blank picture in the League tab only. Two independent causes: (a) `avatars.ts` cached a _rejected_ `api.headshot` as if it were the answer "no photo", so one network blip blanked that face for the whole session — the on-disk cache held a valid 24 KB `2216.img` and had not been written to in a day, because the frontend never asked again; (b) both League-tab call sites passed `team={null}` (the `TradeIdea` and `ActivityItem` payloads carried no NFL team), so a player with no photo got an empty circle instead of the team logo every other screen falls back to. | (a) `remember()` caches only real answers: a resolved `null` stays cached (Sleeper genuinely has no photo), a rejection is dropped from the map so the next mount or tab switch re-asks — no retry timer, no storm. (b) `TradeIdea` gained `get_team`/`give_team`, and `ActivityItem.player_ids` became `players[{id,name,team}]` — the name also fixes the zoom caption, which read as the literal word "player". Season schema 1.1 → 1.2 on both sides, fixture patched. | `avatars.test.ts` — rejection not cached / resolved null cached / url cached; `SeasonTabs.league.test.tsx` — a face with no photo renders `avatar-logo`, not `avatar-blank`; `season_trades.rs`, `season_activity.rs`, `season_view_more.rs` |
| 7   | `chat::wire_tests::an_error_body_is_unwrapped_into_the_message_the_panel_shows` failed once in a full `cargo test --all-targets` run (2026-09-01) and passed on rerun; 120 further runs under CPU saturation and six parallel test binaries did not reproduce it. The stub server did one `read` of the request and then answered and dropped the socket — if the body had not all arrived yet, the close sends a TCP reset and the client reports "could not reach" instead of the 401 body.                                                                                                                               | Stub now drains the request until the `Content-Length` it promised has arrived, then half-closes after writing. Cause is inferred, not observed; if it recurs the assertion message is the thing to capture.                                                                                                                                                                                                                                                               | `chat_wire_tests.rs` — all 8 pass; watch item                                                                                                                                                                                                |

Notes:

- `fixture_shape.rs` compares key **paths**, so an empty-but-present array contributes nothing and passes. The new test is the cheap half of the missing check: every side of a real trade received something. A general "derived array the code would now populate" guard would mean replaying the fixture's transactions through the view builder, which needs the real league's players and rosters — the thing that file explains it cannot regenerate.
- Bug 6 also fixed a false blind spot in `fixture_shape.rs`: `activity[].players[]` is empty on lineup-gap rows and populated on transaction rows, which the old `shape()` reported as an unpinnable section. A prefix the same document populates _somewhere_ is not a blind spot, and is no longer treated as one.
- Bug 1 was invisible to every checked-in fixture and to the Playwright suite; it needed a 5-snapshot season replay to surface at all.

---

## Earlier history

The Claude Design import notes, sections A to G, verification, merge, improvement waves 1 and 2, and debug pass 2 moved to [docs/TRACKER-ARCHIVE.md](docs/TRACKER-ARCHIVE.md).

## Grade 3 (2026-09-03, after Yahoo lanes) — nine read-only auditors

Fresh numbers: fe 484 tests / 95.5% stmts; rust 702 tests / 91.3% lines / 87.8% fns; Playwright 16/16; verify:fast green; CI + End-to-end green on `5293e20`.

| Area                    | Grade (prev) | Top verified findings                                                                                                                                                                                                                                                                                                                                                          |
| ----------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Draft screen/engine     | A− (A−)      | `Panels.tsx:209` "Won't last to X" names the wrong pick at snake turns (backend prices the pair as one window, `view_signals.rs:46`); no UI button for `refresh_picks`; failed league switch leaves live sync off (`draftSession.ts:231`); keepers behind the clock invisible on a fresh install.                                                                              |
| Drafting-advice quality | B− (new)     | Superflex: 2nd QB −25 and 3rd QB dropped (`recommend_score.rs:164`); TE-premium `bonus_rec_te` never reaches points (`scoring.rs:74`); `market()` uses raw pick not `market_pick` in keeper leagues (`view.rs:224`); unconditional +9 RB/WR head start (`:193`); two silent score terms (`add_silent` :157, :262); second opinion is half-PPR ranks applied to any format.     |
| Season                  | B+ (A−)      | Week never advances while running (`nfl_state` only in `load_season`, `season_engine.rs:210`); win odds are projection-only all Sunday (`season.rs:276`); start/sit calls survive kickoff (`season_calls.rs:145`); refreshed analysis dropped by `should_emit` on 0–0 midweek (`poll.rs:180`); playoff-week odds hard 1.0/0.0.                                                 |
| Chat                    | C+ (B)       | Beta name sent in the JSON body, no `anthropic-beta` header (`chat.rs:202,326`) — needs one live request to confirm; cached system block ~450 tokens, under the cache minimum; negative budget disables the cap (`ChatSessionBar.tsx:82`); key save writes stale config clone (`secrets.rs:165`); spend keyed by screen not league.                                            |
| Backend/data            | A− (A−)      | 8s client timeout vs 14.6 MB players download (`sleeper.rs:249`); fixed `.tmp` name lets concurrent writers corrupt a cache (`engine.rs:230`); no fsync before rename (`cache.rs:91`); sync fs under `loaded` lock every tick (`commands_draft.rs:406`); offline season load fails hard; no poll backoff.                                                                      |
| Tests                   | A− (A−)      | Two tautological `available()` tests (`secrets_args.rs:65`, `yahoo_secrets_tests.rs:219`); `waitForTimeout(9000)` in `draft-board.spec.ts:137`; `bin/` 611 lines untested; no Yahoo fixture drift guard; `ask_claude` never exercised end to end.                                                                                                                              |
| Integration/UX          | A− (B)       | Yahoo-only user cannot get past first launch (`App.tsx:140`, Setup is Sleeper-only); "Connecting to Sleeper" shown for Yahoo leagues (`Panels.tsx:103`); failed season retry invisible once loaded (`session.ts:163`); `xhigh` lowercase in effort control.                                                                                                                    |
| Release/ops/security    | A− (C)       | Unsigned/unnotarized dmg with no README note; no LICENSE on a public repo; no `dependabot.yml`, 4 open alerts (1 Rust: glib); version not shown in UI; logging is four `eprintln!`s.                                                                                                                                                                                           |
| Yahoo                   | B+ (new)     | Auction detected only by `draft_type=="auction"`, `is_auction_draft` unread (`engine_yahoo.rs:305`); unmapped stat ids dropped silently (`yahoo_map.rs:113`); pending OAuth state consumed before exchange so a mistyped code forces restart (`commands_yahoo.rs:262`); no `scope` param; `Debug` derives on secret structs; 80 sequential pool pages, HTTP 999 not retryable. |

Overall: **B+ / A−**. Engineering discipline is high everywhere; the misses cluster in the two newest surfaces (Yahoo first live contact, chat API wire) and in the recommender's hard-coded 1QB/1TE assumptions.

---

## Phone companion (requested 2026-09-03, after the grade-3 fix lanes)

Ask: a website you open on a phone that pairs with the running desktop app, shows the chat session plus recommendations/pick history, and can send messages into the same chat, so one person is on the desktop and another on a phone at the same time.

Design (assumptions stated; change on request):

- **Hosting: the desktop app serves it.** A small HTTP + WebSocket server (axum on the tokio runtime already in the tree) inside the Tauri process, LAN only, OFF by default. Settings → "Phone companion" turns it on, shows the URL and a QR code; a random pairing token lives in the URL, rotated on every enable. No cloud, no accounts, no data leaves the network. Off-network use: run Tailscale on both devices, unchanged.
- **Shared chat lives in the backend.** New "Shared" session per league owned by Rust (persisted under the data dir), broadcast over WebSocket to every client (desktop webview subscribes via the same event). A question from the phone goes through the same `ask_claude` path with the desktop's key/CLI and the same budget cap; the answer lands on both screens. Desktop-only sessions stay as they are.
- **Phone page** (mobile-first, served from the app, no install): current recommendation cards with reasons, on-the-clock strip, last N picks, my roster, sync status; chat thread with composer. Season screen: this week's calls + shared chat.
- **Security:** token required on every request and the socket; server binds all interfaces only while enabled; rate-limited chat posts; CSP on the served page; no Sleeper/Yahoo/Anthropic secrets ever cross to the phone.

Lanes: (1) Rust server + shared-chat store + events, (2) phone page (separate Vite entry, bundled via include_str), (3) desktop Settings pairing panel + Shared session in the chat panel, (4) tests: axum wire tests, Playwright mobile viewport against fixtures.

- **Second desktop by IP (added same day).** The desktop app gains "Join another Draft Assistant…" (Settings and first-launch screen): enter `host:port` and the pairing code shown on the host. In follower mode the app's `Api` is a remote implementation over the same HTTP + WebSocket the phone uses (api.ts already swaps implementations for the browser preview), so the full desktop UI — board, clock, season, shared chat — renders the host's data live; league switching, keys, and settings stay on the host and the follower's controls for them are disabled with a "hosted by <name>" pill. Leave follower mode from the same menu. Host lists connected devices and can revoke the code (rotates the token, drops every client).

Status (2026-09-04): **built and verified live.** Commit `2031ae5` + polish. Headless host `cargo run --bin companion_host -- <league_id> [--port N]` (launch config "companion") serves the phone page over a real league; verified on Sharks: pairing with the code, "Rob's iPhone" listed under devices, `/api/config` free of secrets, socket connected, Week tab hidden with no season, Balanced/Safe/Upside cards labelled. Not exercised live: a shared-chat question (it would bill the real key); covered by `tests/companion_wire.rs` over the budget-cap path. The desktop UI toggle was not driven (screen access declined); its commands are covered by `command_surface.rs` and the panel by RTL tests.

---

## Grade 4 (2026-09-04, after the companion, dogfood and dependency work) — ten read-only auditors

Fresh numbers: fe 592 tests / 92.7% stmts; rust 952 tests / 90.7% lines / 87.4% fns; Playwright 33/33; wdio smoke green; CI + End-to-end green on `417bfa3`; zero open PRs; 2 Dependabot alerts left, both without a fix (extract-zip in the e2e dev tree, glib via Tauri on Linux only).

Every grade-3 finding was re-checked: 40 of 44 fully fixed, 4 partial (season poller has no backoff, rosters have no offline cache, Yahoo pool pages still sequential, keepers still invisible on a first launch mid-draft), 1 not fixed (`bin/` untested, now 759 lines).

| Area                             | Grade (prev) | Top verified findings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Draft screen/engine              | A− (A−)      | Survival window collapses only a _pair_ of adjacent picks, so three back-to-back picks or a keeper between two picks target your own next pick and every player reads ~99% to survive (`view_signals.rs:47`, mirrored `Panels.tsx:390`); keeper branding is permanent on disk from one `/picks` snapshot, a single missing pick brands every later pick a keeper with no way to clear (`picks.rs:84`, `keepers.rs:47`); clock ignores `start_time` so 1.01 shows nothing or a stale 0:00 (`view_signals.rs:28`); a paused draft looks live (no `paused` handling anywhere); a failed `/draft` call counts as a failed tick and stretches polling to 24s while picks fly (`commands_draft.rs:387`).   |
| Drafting-advice quality          | B (B−)       | Season-ending injury is a flat −25 against `0.6×VORP`, so a PUP RB1 is still the round-1 pick (`recommend_score.rs:433`); SUPER_FLEX demand split four ways credits QB 0.25, board says thinner at RB than QB in a superflex draft (`:186`, disagrees with `valuation.rs:109`); `starters_phrase` renders demand 1.25 as "about 2" so the reason contradicts the arithmetic (`:166`); tier thresholds are absolute points with no scoring normalisation, so TE-premium/6-pt-TD leagues fire tier alerts all night (`valuation.rs:161`); "falling" bonus flat +5 at 9 or 90 picks past ADP (`:338`).                                                                                                  |
| Season                           | B+ (B+)      | Auto week rollover never calls `record_history`, so an app left running over Tuesday empties the Trends tab and loses that week's snapshot permanently (`poll.rs:334`, `season_engine.rs:353`); injuries and weekly projections frozen for the process lifetime, Saturday inactives never land (`commands_draft.rs:114`); playoff odds still ignore live scores, the class just fixed for win odds (`season_view_standings.rs:49`, `season_odds.rs:250`); header defaults to "best lineup" odds after lock (`SeasonScreen.tsx:187`); whole-league deep clone every tick (`state.rs:199`).                                                                                                            |
| Chat                             | B (C+)       | **API calls inherit the Sleeper client's 8s total timeout** — any real Opus answer with thinking is aborted, tests miss it because the stub answers instantly (`commands_chat.rs:336` → `sleeper.rs:254`; verified by hand); cache breakpoint sits after the volatile clock/pick block so draft night is all cache writes, no reads (`chat.rs:300`, `chat_context.rs:113`); `stop_reason: max_tokens` unchecked, truncated answers render as finished (`chat.rs:394`); fallback answers priced as the requested model (`commands_chat.rs:328`); no in-flight guard, two concurrent questions both pass a cap one exhausts (`:305`).                                                                  |
| Backend/data                     | A− (A−)      | Season poller sleeps a flat interval regardless of failures it already counts (`commands_season.rs:200`); `rosters` is the one uncached hard failure in `load_season`, bypassing the offline fallback (`season_engine.rs:275`); `LoadedLeague` deep-cloned every tick and per chat question (`state.rs:206`); `export_state` writes non-atomically and 0644 (`commands_draft.rs:304`); cold players load can block ~6 min with two 14.6 MB maps resident (`projections.rs:42`).                                                                                                                                                                                                                      |
| Tests                            | A− (A−)      | `Markdown.tsx:19` link-as-span safety invariant has zero tests; `public/dev-fixture.json` is a fourth schema copy outside `replay_contract.rs`; `command_surface.rs:268` makes real Sleeper calls and binds a LAN port from the unit suite; 19 of 22 follower host-only refusals untested (`apiRemote.ts:272`); `yahoo_secrets_tests.rs:242` no-ops on macOS so the FileStore fallback never runs in CI; `bin/` 759 lines, zero tests.                                                                                                                                                                                                                                                               |
| Integration/UX                   | A− (A−)      | Sleeper wording on two Yahoo-visible surfaces (`Panels.tsx:252`, `Overlays.tsx:59`); "Error: Error:" on the launch screen (`draftSession.ts:177`); two toasts bypass the timer and never auto-dismiss (`App.tsx:99,106`); Enter does nothing on the first-launch form (`Panels.tsx:57`); Yahoo lookup failure lands in Sleeper's error slot with a Sleeper-only retry (`LeaguePicker.tsx:102`).                                                                                                                                                                                                                                                                                                      |
| Release/ops/security             | B+ (A−)      | Pairing lockout is global not per-peer, so one LAN peer spamming `/api/pair` blocks pairing for everyone (`hub.rs:186`); no Origin/Host check + `Access-Control-Allow-Origin: *` = DNS-rebinding reach to `/api/pair` (`routes.rs:47`); pairing code never rotates or expires (`hub.rs:98`); plaintext HTTP with the token in `?token=` on the socket URL (`ws.rs:19`, `app.js:163`); phone page ships no CSP (verified: no header, no meta); no release workflow, tags, or updater — dmg is a 14-day CI artifact. Grade fell because the new server widened the surface, not because anything regressed.                                                                                            |
| Yahoo                            | B+ (B+)      | Backoff 250/500 ms, no `Retry-After`, and a mid-pool 999 discards all 59 good pages then restarts at page 0 into the same throttle (`yahoo.rs:253,369`, `engine_yahoo.rs:72`); pagination stops on `players.len()` post-filter so one malformed row silently truncates the pool, `PlayerPage.count` parsed and never read (`yahoo.rs:370`, `yahoo_parse.rs:310`); auction is detection-only, `auction_costs` never called from the engine; stat id 15 (Return TD, individual) mapped to `st_td` (DEF unit) with no warning (`yahoo_map.rs:41`); token refresh holds the mutex across a 10s round trip and N concurrent calls each refresh (`yahoo.rs:194-220`); keepers never detected.              |
| Phone companion + follower (new) | B− (—)       | Host restart mints a fresh hub with no persisted tokens; phones sit on "Reconnecting…" forever, follower retries silently (`lib.rs:148`, `hub.rs:94`, `app.js:108`, `apiRemote.ts:122`); nothing re-reads state after a reconnect, opening frames are devices + chat only (`ws.rs:102`); the pick clock never counts down — computed once per render, no interval (`app.js:221`); every iPhone defaults to the name "iPhone" and the dedupe evicts the previous one with no `revoked` frame (`helpers.js:17`, `hub.rs:203`); a hung answer leaves `busy` set for everyone with no timeout (`shared_chat.rs:180`, `routes_chat.rs:151`); follower has no connection state at all (`apiRemote.ts:66`). |

Overall: **B+**, unchanged. The grade-3 list was executed almost in full and the fixes are tested, so the older surfaces held or rose (chat C+→B, advice B−→B). The score is held down by the two newest surfaces: the companion's liveness (stale clock, no re-sync, silent unpair) and the attack surface it opened (global lockout, no origin check, non-rotating code), plus one hard blocker found in chat — the inherited 8-second API timeout, which makes the API provider unusable for real answers while every test stays green.

Cheapest high-impact fixes, in order: chat timeout (one builder call); companion `setInterval` for the clock + re-fetch on reconnect + per-IP lockout buckets; `record_history` on auto rollover; survival window over unmade picks; Yahoo pagination on `count` with `Retry-After` backoff.

### Grade-4 fix batch (2026-09-04) — seven lanes, one commit

All 15 headline items plus the three honourable mentions landed, verified on the combined tree: fe 652 tests, Rust suite green, Playwright 34/34, verify:mid clean.

- **Chat:** dedicated Anthropic client (10 min total timeout) replaces the borrowed 8 s Sleeper client; `max_tokens` truncation appends a note; fallback answers priced by the model that answered; one API question in flight per league; shared chat answers time out at 5 min and always clear `busy` (drop guard).
- **Companion:** devices, tokens and code persisted 0600 so a host restart keeps phones paired; bad token closes the socket 4401 and the phone/follower show "pair again"; opening frames now carry draft + season snapshots; 1 s clock ticker on the phone; same-name phones get "iPhone 2" instead of eviction, same device_id re-pairs; per-IP pairing lockout; code rotates after each pairing and on a 10 min idle timer (Settings re-reads it); CSP + nosniff + no-referrer on every static response; Origin check on state-changing routes; `season-poll-health` handled.
- **Follower/UX:** connection state pill (reconnecting / unpaired + Pair again); re-fetch on every socket open; "Error: Error:" gone; toasts through one timer; Yahoo wording in the mark-drafted confirm; separate Sleeper/Yahoo error slots and retry in the league picker; all 20 host-only refusals table-tested.
- **Draft engine:** survival window runs through every consecutive pick that will not actually be made (mine or keeper); keeper inference only pre-draft or below the load-time floor, never off the board, `clear_keepers` command + Settings row; clock uses `start_time`; `paused` on the wire and in the banner; a failed `/draft` call no longer counts as a failed tick.
- **Season:** auto rollover records history; injuries + weekly projections refreshed every 30 min and on week change; playoff odds priced live like win odds; header shows set-lineup odds once locked; `Arc` on the big `LoadedLeague` fields so the tick clones pointers; calls count and points agree; "Locks in" ticks.
- **Advice:** injury term proportional to value (IR/PUP ~all season); superflex demand allocated by value; honest `starters_phrase`; tier gap scaled to the league's point scale; falling/reach proportional with caps; discipline defers to need on the same hole.
- **Yahoo:** Retry-After + exponential backoff, per-page pool cache with resume, walk driven by `count`; single-flight token refresh; auction budget/costs via `yahoo_auction`; keepers parsed. Stat 15 kept as `st_td` (it is the individual return TD in Sleeper's key space; DEF unit TDs are `def_*`).
- Deviations accepted: keeper rule "beyond total_picks_made" replaced by the board bound (the literal rule discards real keepers); Panels.tsx:252 Sleeper wording still open.

---

## Grade 5 (2026-09-05, after the grade-4 fix batch) — ten read-only auditors

Fresh numbers: fe 653 tests / 92.7% stmts; Rust suite green (627 lib + integration targets); Playwright 34/34; CI + End-to-end green on `2597a87`. Open PRs: #22 vite 8 + plugin-react 6 (grouped, green), #21 lxml floor (green). Under heavy local load (ten auditors running cargo/vitest at once) 10 frontend tests and one Rust process test timed out; all pass idle, five runs in a row for the Rust one. Logged under Tests as a real flakiness item, not as a red suite.

Every grade-4 headline fix was re-verified by a fresh auditor: 40 of 41 confirmed real and tested. The one exception is Yahoo keepers, which parse a field the live API does not send.

| Area                       | Grade (prev) | Top verified findings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Draft screen/engine        | B+ (A−)      | Traded picks fetched only at load, so an in-draft trade is invisible until a full refresh (`tick.rs:90`; Yahoo hardcodes none, `engine_yahoo.rs:194`); the final pick of the draft never reaches the activity feed or run window (`view.rs:39,194`, `current_pick = teams*rounds` filtered with `<`); keeper league in `pre_draft` shows a manager on the clock hours early because keepers make `total_picks_made` non-zero (`ClockBanner.tsx:39,94`); failed disk writes recorded as failed poll ticks, the `/draft` fix one layer down (`commands_draft.rs:346,352`); re-pull button toasts success on the `EMPTY_PICKS` path (`draftSession.ts:232`). Lower: blind `pop()` on undo (`tick.rs:108`), twice-traded pick names the intermediate owner (`traded_picks.rs:78`), chime fires on a paused draft (`App.tsx:150`). |
| Drafting-advice quality    | B+ (B)       | `roster.rs:83` hands SUPER_FLEX to the position with the most spare bodies, so a spare WR eats the slot and QB2 is scored as a "depth pick" while WR4 gets +8 for "filling SUPER_FLEX" (the passing test hand-writes `open_starters`); `dedicated_starters` special-cases only QB, so TE-premium REC_FLEX leagues still dock TE2 −20 and drop TE3 (`recommend_demand.rs:84`, `recommend_score.rs:199`); Safe mode charges reach twice, the second uncapped (`recommend_score.rs:329,358`); demand computed from the remaining pool, so QB demand collapses exactly when QBs are scarce (`recommend.rs:202`, `valuation.rs:122`); pre-draft gate drops only "Questionable", not "Doubtful", and weekly `OUT` is priced as a quarter season (`recommend_injury.rs:40,62`).                                                      |
| Season                     | A− (B+)      | "locked" fires at Thursday kickoff if the lineup is already optimal, hiding the toggle for five days (`SeasonScreen.tsx:248`); playoff odds bank the _optimal_ lineup's points, not the set one, so standings and scoreboard disagree (`season_view_standings.rs:67`); the Refresh button never rolls the week, only the poller does (`commands_season.rs:115`); a just-finished week vanishes from both sides until Sleeper settles standings (`season_view_standings.rs:52`, `season_odds.rs:157`); `is_usable()` on a refreshed dictionary only checks non-empty, so a truncated parse blanks every injury (`season_refresh.rs:35,66`).                                                                                                                                                                                    |
| Chat                       | B+ (B)       | After any failed shared turn the failed question stays while its error is filtered, so the next call sends two consecutive user messages and the API rejects with "roles must alternate", permanently (`shared_chat.rs:238`); `MAX_TURNS=60` vs `MAX_ENTRIES=200` with no way to start a new shared thread (`commands_chat.rs:161`); thinking summaries requested, paid for, never shown or replayed (`chat.rs:441`, `Chat.tsx:219`); `MAX_TOKENS=16000` non-streaming so Max effort truncates and bills in full (`chat.rs:21`); spend key `active_league_id` vs panel `chatScope` mismatch shows $0 until refused (`commands_chat.rs:317`). Still open from grade 4: cache breakpoint on the volatile block.                                                                                                                 |
| Backend/data               | A− (A−)      | Season poller still has no backoff (`commands_season.rs:200`); rosters still the one uncached hard failure (`season_engine.rs:277`); `export_state` still bare `fs::write` 0644 (`commands_draft.rs:244`); new: `write_cache_off_thread` serialises the 15 MB players map on the runtime thread before handing bytes to `spawn_blocking` (`engine.rs:278`); `season_inputs` still deep-clones all of `LoadedSeason` (`state.rs:223`); lineup solve + playoff sim run before `should_emit` every tick (`poll/season_loop.rs:246`); headshots share one `.img.tmp` per player (`headshots.rs:144`); `config.json.bak` copy unchecked and unsynced (`engine.rs:352`).                                                                                                                                                            |
| Tests                      | B+ (A−)      | Suites flake under load: `chat_cli_process_tests.rs:206` kills the fake CLI before it writes its pid (auditor reproduced 1 in 3 under load); ten vitest timeouts at 5 s/20 s in App/Board tests when contended, CI runs under coverage instrumentation; coverage floors 80/70/75 vs actual 92/88/89 and Rust 88/85 vs 90/87 guard nothing; `clear_keepers` has no behaviour test (`edits.rs:98`); two test names assert more than the test does (`store.rs:101`, `yahoo_retry_tests.rs:92`); `names.rs` untested directly; still open: Markdown link-as-span invariant untested, `command_surface.rs` non-hermetic, `bin/` 557 lines. Fixed since: dev-fixture drift guard (`fixture_shape.rs`), follower refusal table, yahoo_secrets FileStore now runs in CI.                                                              |
| Integration/UX             | B+ (A−)      | `describeError` shipped but five call sites kept `String(e)` including the first-launch form (`Panels.tsx:43`, `Chat.tsx:244,260`, `SharedChat.tsx:61,90`, `ChatKeyForm.tsx:36`); first-launch dialogs don't trap focus (`useFocusTrap.ts:56` inerts `.shell`, which the setup screen never renders); the setup screen is a one-way door with no Back (`App.tsx:250`); `setMyUsername` reachable only on first launch while the roster panel tells you to set it (`Panels.tsx:38,256`); "Headshots from Sleeper" row shown to Yahoo users (`settingsRows.ts:166`). Still open: Enter on the first-launch form, `role="alert"` on six error blocks, chat collapses below the board at the 1000 px minimum with no cue.                                                                                                         |
| Release/ops/security       | B (B+)       | CSP `connect-src 'self' ws: wss:` is a scheme source, so injected script can open a socket to any host with the token; no `frame-ancestors` (`routes.rs:47`); Origin allow-list accepts any RFC1918/CGNAT host on the port, not the bound address (`net.rs:102-119`); idle code rotation gated on `devices.is_empty()` and driven only from `companion_status`, so it never runs with a phone paired or the panel closed (`hub.rs:192,203`); no tags, release workflow, or updater, so none of these fixes can reach an installed user; device tokens + code persisted as plaintext JSON with no integrity check while the API key uses the Keychain (`store.rs:17-44`). Still open: plaintext HTTP with `?token=`.                                                                                                           |
| Yahoo                      | A− (B+)      | Keepers parse `is_keeper` off `draft_result`, which the live `draftresults` resource does not carry; the roster fallback reads `pool`, which has no flag either, and `team_roster` (`yahoo.rs:422`) has no production caller (`yahoo_parse.rs:326`, `yahoo_map.rs:264`); `draft_budget` likewise fixture-only (`yahoo_parse.rs:223`); poll ticks pass an empty player map so `metadata` and `is_keeper` flip to `None` on the first tick after load (`commands_yahoo.rs:183`); offset resume against a mutable pool skips players and still marks `complete` (`engine_yahoo_pool.rs:51`); DEF crosswalk has no name fallback (`yahoo_crosswalk.rs:136`).                                                                                                                                                                      |
| Phone companion + follower | B+ (B−)      | The server never restarts itself: no `companion_enabled` flag persisted and `lib.rs:150` never calls `start`, so a host crash leaves every phone on "Reconnecting" until someone re-ticks Settings, largely undoing token persistence; heartbeat is write-only, nobody reads `pong` or times out, so a sleeping Mac leaves a frozen board labelled online (`app.js:179`, `apiRemote.ts:99`); no `visibilitychange`/`online` listener on the phone; follower omits `device_id` from the pair body so each rejoin adds "This Mac 2, 3…" (`companion.ts:121`); a re-pair leaves the old token's socket fully authorised because gates check `device_id` not token (`ws.rs:71,101`, `hub.rs:311`).                                                                                                                                |

Overall: **B+**, unchanged for the third card. The batch was real: 40 of 41 fixes verified with tests, and four areas rose (advice, season, chat, Yahoo, companion). Three fell (draft screen, UX, tests, security) for the same reason each time: a fix applied to the instance rather than the class. `describeError` landed and five call sites kept the old pattern; `platformName` landed and one settings row kept "Sleeper"; the `/draft` tick fix landed and disk writes still count as failed ticks; the CSP landed with a scheme source that admits every host; token persistence landed without autostart, so a crash still strands every phone.

Cheapest high-impact fixes, in order: companion autostart + `pong` timeout + `visibilitychange` (three small pieces that finish the liveness story); CSP `connect-src` one line + drive code rotation from the poll loop; shared-chat role alternation (drop the failed question with its error); traded picks in the tick; the five `String(e)` sites + the Headshots row; pre-draft banner in keeper leagues; SUPER_FLEX assignment by value in `roster.rs`; raise coverage floors to five under actual and fix the pid-file race.

Addendum (draft frontend sub-audit, arrived after the card): double-tapping "Mark drafted" fires `recordManualPick` twice, the second fails "player already drafted" and leaves a non-dismissing retry toast on a pick that was fine (`App.tsx:159`, `Overlays.tsx:71`, no in-flight state on the auto-focused button); the snake strip keeps a phantom on-the-clock chip and "1 picks ahead" after the draft completes (`ClockBanner.tsx:207`); `switchLeague` applies the new view before `getConfig`, so a config failure reports "could not switch" after the switch visibly happened (`draftSession.ts:266`); season stop/start on league change are two unawaited invokes and `stop_season_polling` never bumps the generation, so the stop can land after the start and freeze live scores silently (`session.ts:138-152`, `commands_season.rs:207`); Pts header unclickable after an import clears the second-opinion column while sorted on it (`Board.tsx:279`); board page size never resets on filter change (`Board.tsx:205`).

Addendum (tests auditor, after the Rust coverage run): Rust is at 90.25% lines / 87.70% fns against floors of 88/85, so only ~2.5 points of room, but `bin/` is 556 lines at 0.00% _inside_ the denominator (backtest.rs 191, backtest/fetch.rs 79, backtest/report.rs 75, companion_host.rs 87, dump_season.rs 59, dump_state.rs 65); strip them and the tested crate is near 93%, so the floor is propped up by dead weight. `lib.rs` is 0.00% including `run()`, while `tests/command_surface.rs:1` claims to be "the one place lib.rs is exercised" (it reads the file as text). `yahoo_secrets.rs` 63.64% fns: the FileStore path runs on macOS now, the keychain path still never does; `secrets.rs` 72%/62% is the weakest file in the crate. `names.rs` is 93.94%, not untested.

### Grade-5 fix batch (2026-09-05) — ten lanes, one commit

All 15 headline items, the three honourable mentions, and a new **Logging / error reporting** area. Verified on the combined tree: fe 752 tests (floors raised to 89/87/84/83), Rust 1087 tests at 92.8% lines / 89.6% fns (floors raised to 90/87), Playwright 36/36, verify:mid clean.

- **Companion:** `companion_enabled` persisted and the server autostarts; phone and follower time out on two missed pongs and reconnect, and revive on `visibilitychange`/`online`; CSP `connect-src` names the bound ws origins (no bare `ws:`) plus `frame-ancestors 'none'`; Origin check against the actually-bound addresses; code rotation driven by a 60 s task regardless of paired count; a re-pair or revoke closes the old token's sockets with 4401; `hello` carries `server_now_ms` for clock skew; follower sends its `device_id` on re-pair.
- **Chat:** failed shared turns dropped from the model transcript so roles alternate; `shared_chat_reset` + `POST /api/chat/reset` + "New thread"; a sliding window instead of refusing at 60 turns; cache breakpoint on the stable prefix (~1.3k tokens) with the clock block last; thinking summaries no longer requested; spend keyed from the loaded league; truncation note suggests lower effort.
- **Draft engine:** traded picks fetched every tick and on re-pull; trade chains resolved to the final owner; final pick reaches the feed (`current_pick = teams*rounds+1`); disk-write failures are notes, not failed ticks; undo removes the named pick; manual picks get the ownership-aware slot; `export_state` atomic and 0600; empty re-pull returns an error the toast explains.
- **Draft UI:** not-started = `pre_draft` AND no non-keeper picks (mock drafts report pre_draft mid-draft); no queue chip after completion, plural fixed; no "yours" glow or chime while paused; Enter submits the first-launch form, `role="alert"`, `describeError` everywhere; "Sleeper username…" settings row opens the setup screen with a Back; Headshots row platform-aware; board sort recovers when a column vanishes, page size resets on filter; chat panel scrolls into view when stacked at 1000 px; Mark drafted has an in-flight state; `switchLeague` only fails on the switch itself; season stop/start serialised; first-launch dialogs trap focus.
- **Season:** locked = every one of my starters kicked off; playoff odds bank the set lineup; Refresh rolls the week; a finished week counts before Sleeper settles it; a partial player dictionary (<80% coverage) is refused and never blanks an injury; season poller backs off; stop cannot kill a later start; rosters cached with offline fallback; `LoadedSeason` big fields `Arc`; solver skipped when nothing moved.
- **Advice:** open flex slot paid to the position the league's allocation is short of (superflex → QB2 over WR4, through the real `open_starters` path); dedicated starters from demand, not slot names (TE-premium); one capped reach term in Safe; demand from the full board so QB need holds after a run; Doubtful ignored pre-draft, weekly OUT priced as one week; need paid once.
- **Yahoo:** keepers read from `teams;out=roster` (the shape the live API sends); budget derived from max team spend when absent, with a warning; poll ticks keep metadata and keeper flags; partial-pool resume limited to 1 h and verified; team-abbreviation crosswalk + DEF name fallback; traded-picks-unread warning; fixture guard records recorded-shape vs hand-written.
- **Tests/hygiene:** pid-file race fixed; Board tests scoped (6 s → 0.25 s); `command_surface` hermetic (stub base, port 0); `bin/` 0% → ~65% with pure arg parsing; Markdown link-as-span test; `clear_keepers` flow test; honest test names; serialisation off the runtime thread; `config.json.bak` synced and checked; headshots temp names unique.
- **Logging / error reporting (new area): D → B.** `applog::{error,warn,info,debug}` with redaction (keys, bearer tokens, `token=`/`code=` values, six-digit codes in URLs) and `context()`; panic hook; `failing(cmd, ctx)` on the nine most important commands; poll-health transitions logged once; `window.onerror`/`unhandledrejection` → `log_frontend_error`; ErrorBoundary reports and offers "Copy details"; Settings → Diagnostics… (version, ids, poll health, companion status, log path, last 200 lines, Copy diagnostics, Open log folder). Still missing for A: season loop on `HealthWatch`, tail across generations, ~30 commands still unlogged, debug level env-only.

**Logging / error reporting: B → A** (`cf9e09d`). Season poller on `HealthWatch` with league/week context; `tail` splices `.log.1` before the current file; every failing `#[tauri::command]` (41) logs an ERROR line naming the command via `applog::logged!`; runtime log level (`set_log_level`, `AppConfig.log_level`, "Verbose logging" in Diagnostics); INFO lines for app start, league load/switch, season load, companion on/off; panic line carries the version. Dependabot #22 (vite 8 + plugin-react 6) and #21 (lxml) merged; verify:mid, `npm run build`, Playwright 36/36 all green under vite 8.

---

## Live-test list (2026-09-05) — needs a person at the machine

Everything here is covered by tests against fixtures or a headless host; none of it has been driven end to end on the real app by the user. Tick off as each is done, and note what broke.

| #   | What to try                                                                                      | Expect                                                                                                                                                                                                | Status                                                                                                                                                                             |
| --- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L1  | Install Tailscale on the Mac and phone, same tailnet. Turn the companion on in Settings.         | Within 30 s the panel shows "Or, over Tailscale, from anywhere" with a `100.x` URL (`c5b481b` refreshes origins live). Phone on cellular scans it, pairs, sees the clock tick.                        | open                                                                                                                                                                               |
| L2  | The native Settings → Phone companion toggle, on a LAN phone.                                    | QR scans, code pairs, device listed, revoke throws it off with a "pairing ended" message. (Only the headless `companion_host` was driven live.)                                                       | open; pairing, restart-survival and the phone page re-driven in the browser pane on 2026-09-05 against the headless host, see below                                                |
| L3  | Ask one question from the phone in the shared chat.                                              | Answer lands on both screens, cost shown, spend counter moves. Bills the real key once.                                                                                                               | open                                                                                                                                                                               |
| L4  | Join from a second Mac with "Join another Draft Assistant…".                                     | Full board and clock render read-only with the "hosted by" pill; leaving returns the local leagues.                                                                                                   | half done: the web preview joined the headless host by `localhost:7878` + code, showed the hosted-by pill, live board and season placeholder; a real second Mac still open         |
| L5  | Live Yahoo: paste client id/secret, connect, load a real league.                                 | Sign-in completes on the local redirect; league, keepers and budget match the Yahoo site. Capture the responses as recorded-shape fixtures (`Source::RecordedShape`) so the hand-written ones can go. | open                                                                                                                                                                               |
| L6  | A Sleeper mock draft start to finish with the app open.                                          | Chime on my pick, Mark drafted once, undo, the last pick reaches the feed, pause banner during a pause.                                                                                               | open                                                                                                                                                                               |
| L7  | A Sunday during the season.                                                                      | Scoreboard live, "locked" only once all my starters have kicked off, Refresh rolls the week Tuesday morning.                                                                                          | open                                                                                                                                                                               |
| L8  | Settings → Diagnostics… on the Mac.                                                              | Copy diagnostics pastes clean text with no code or token; Open log folder lands in the right place; Verbose logging changes the tail.                                                                 | dialog and the copy-failure message checked in the browser preview; the Mac-only actions still open                                                                                |
| L9  | Download the CI `.dmg` on a Mac that has never run the app.                                      | Gatekeeper warning (unsigned) is the only obstacle; first launch reaches the setup screen.                                                                                                            | dmg inspected: 8 MB, aarch64 only, ad-hoc signed with hardened runtime, `spctl` rejects it, so macOS 15 needs Settings → Privacy & Security → Open Anyway; first launch still open |
| L10 | Open the https tailnet URL on the phone (needs HTTPS certificates enabled in the tailnet admin). | Padlock in the address bar, Add to Home Screen offered and the installed app opens standalone, and the screen stays awake during a live draft.                                                        | open                                                                                                                                                                               |

## Still missing (2026-09-05)

Code work, cheapest first. Nothing here is blocked on a live test.

1. **Tagged release workflow.** Nothing built on CI ever reaches an installed user; there is no tag, changelog or updater. Add a `v*` tag workflow that builds the `.dmg` and attaches it to a GitHub release. Real signing and notarisation need the user's Apple developer account; ship unsigned first.
2. **Companion tokens and pairing code into the Keychain** instead of the 0600 JSON store, via the same `/usr/bin/security` path Yahoo uses.
3. **Skip publishing when nobody is subscribed.** `hub.publish` serialises every draft and season snapshot on every tick even with the companion idle.
4. **MagicDNS name in the QR** from `tailscale status --json`, so the URL survives a tailnet address change.
5. **`tailscale cert` HTTPS.** Serve the tailnet's real certificate when present: a padlock on the phone, and PWA install and wake lock, which need a secure context. Roughly a day; pulls the rustls chain in.
6. **Grade 6 regrade** after the release workflow lands.
7. Deferred from earlier cards: last-season narrative (needs bracket data), the two Dependabot alerts that cannot be fixed upstream (extract-zip in the e2e dev tree, glib via Tauri on Linux).

### Pre-tests run without the user (2026-09-05, after `127d12c`)

- **Found and fixed a real bug in the Keychain path.** The `security` tool's password prompt keeps 128 bytes and silently drops the rest, and every Keychain write in `yahoo_secrets.rs` went through that prompt. The companion's device list came back as a 128-byte stub after a host restart (every phone unpaired), and a Yahoo token set is longer still, so live Yahoo sign-in would never have survived a restart either. The Anthropic key is under 128 bytes, which is why `secrets.rs` never showed it. Fix: the value travels as a hex-encoded argument (`-w <hex>`), decoded on read; plain legacy items still read. Verified live: pair a phone, restart the headless host, the phone is still paired and the Keychain item parses with both devices.
- Phone page against the headless host: pairing by code, live board, restart survival, second device listed. Follower join from the web preview: hosted-by pill, live Draft board, season placeholder with Try again when the host has no season loaded.
- Diagnostics dialog in the browser preview renders every row and hides the log actions; a refused clipboard write shows its error in the dialog rather than nothing.
- CI dmg inspected (see L9). The Mac's LAN address moved from 192.168.1.242 to 192.168.2.10 since yesterday, which is exactly the case the origin refresh and the MagicDNS name are for.
- Note: the headless `companion_host` and the desktop app share one Keychain item, so running both on the same Mac makes them overwrite each other's device list. Fine for a test host, worth a `--data-dir`-scoped account if the headless host becomes a real deployment.

---

## Grade 6 (2026-09-05, after the release/Keychain/MagicDNS batch and the Keychain 128-byte fix) — eleven read-only auditors

Fresh numbers at `69665f3`: Rust 1118 tests, 91.9% lines / 89.6% functions (floors 90/87); frontend 757 tests, green in the pre-commit run for `69665f3`; Playwright 36 in 3 files. Two frontend runs during the audit showed 6 to 12 timeouts at the 5 s default, on a machine whose load average was 40 to 70 from the auditors plus an unrelated Android emulator and KiCad render. Same symptom as grade 5; the tests auditor found the cause this time (item T3 below) rather than the suite being red.

Prior claims: every grade-5 fix and every "pre-test" claim was re-verified by a fresh auditor. Two did not hold as written: "Verbose logging changes the tail" (nothing writes a debug line) and `command_surface` "hermetic" (network yes, Keychain no).

| Area                       | Grade (prev)   | Top verified findings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Draft screen/engine        | A− (B+)        | Keeper floor guard skipped entirely in `pre_draft`, so a dropped `/picks` row in a mock draft brands every later pick a keeper on disk (`keepers.rs:110`); a partial `/picks` answer rewinds `current_pick` to the hole, wrong manager on the banner (`picks.rs:73`); three "Mark drafted" cards after the draft ends (`recommend.rs:206`); `recorded` set never cleared on league switch (`markDrafted.ts:56`); `final_owner` follows the first link in a re-traded chain (`traded_picks.rs:74`); `view_now` skips the draft-id check (`tick.rs:184`).                                                                                                                                                                                               |
| Drafting-advice quality    | B+ (B+)        | IDP leagues: DL/LB/DB never reach the board and their slots count as open starters all draft (`roster.rs:11`); two injury dictionaries, "Suspended" priced as six points (`recommend_injury.rs:47` vs `season_injury.rs:19`); reach term asleep until 25 picks ahead (`recommend_score.rs:368`); reason slot one always the VORP line already shown in the stats (`Panels.tsx:53`); upside term one-directional (`:484`); `weeks_left` dead in production (`view.rs:250`).                                                                                                                                                                                                                                                                            |
| Season                     | B+ (A−)        | A one-week injury tag zeroes the player for the rest of the season in odds and Trends (`season_lineup.rs:127`); rollover fires Monday night off `display_week`, making the finished-week path unreachable live (`season_api.rs:28`, `season_loop.rs:339`); IR never consulted (`Roster.reserve` unread); "player list incomplete" warning permanent (`season_loop.rs:290`); empty/bye starters read as locked (`season_view_live.rs:44`); odd-team leagues double-count a bye week (`season_view_standings.rs:140`); waiver pool truncates bye players (`season_view_market.rs:43`).                                                                                                                                                                  |
| Chat                       | B+ (B+)        | Cached prefix still rewritten every pick (recent picks, 40-row board in `draft_stable`, `chat_context.rs:200,270`), untested; shared answer past 300 s billed, discarded, and never counted (`routes_chat.rs:25`, `commands_chat.rs:471`); `MAX_TOKENS=16000` non-streaming still; league/team names unsanitised into the system prompt (`chat_context.rs:165`); `secrets.rs` still on the truncating stdin prompt and reads hex literally (`secrets.rs:78,93`).                                                                                                                                                                                                                                                                                      |
| Backend/data               | B+ (A−)        | A `null` Sleeper payload blanks the live season and stamps the source green (`season_api.rs:328`, `season_sources.rs:88`), the draft loop's `EMPTY_PICKS` guard never reached the season loop; an empty week cached forever at `ttl=u64::MAX` (`season_engine.rs:142`); a tick can take 25 s with a green badge because note-only calls retry 3×8 s (`tick.rs:139`, `sleeper.rs:19`); unparseable config.json silently resets everything (`engine.rs:259`); `build_view` clones the board under both mutexes on the runtime thread (`poll_loop.rs:171`); headshot temps never swept; `applog::append` synchronous per line.                                                                                                                           |
| Tests                      | B (B+)         | `command_surface.rs:324` uses `YahooState::default()`, the real Keychain, and invokes `yahoo_disconnect` → `clear_tokens`: `cargo test` deletes a live Yahoo token (none exists yet); vitest floors gate only files that tests import (no `coverage.include`), so deleting a screen's tests raises coverage; `testTimeout` never raised so the 5 s `waitFor` budget is unreachable (`App.test.tsx:50`); `applog` capture buffer is process-wide, a live flake source (`applog.rs:266`); all thirteen Yahoo fixtures hand-authored, ten labelled `RecordedShape`; CI never loads the production bundle; `store_tests.rs:100` cannot fail; `check:version` and `test:scripts` never run in CI.                                                          |
| Integration/UX             | B+ (B+)        | Season tab offered to Yahoo leagues, which hits Sleeper with a Yahoo key and fails forever (`Header.tsx:319`, `commands_season.rs:60`), plus "Sleeper username" copy across the season screen; one ErrorBoundary for both screens, a crash on one shows on the other (`App.tsx:371`); follower hangs on the launch screen with no controls when the host stops answering HTTP (`apiRemote.ts:57`, no timeout); "Sleeper username…" row leads to a form that needs a league id (`SetupScreens.tsx:51`); Back button names the wrong league after a switch (`App.tsx:172`); clipboard copies silent or stale; `role="alert"` missed in three places; em-dashes throughout UI copy.                                                                      |
| Release/ops/security       | B+ (B)         | Still no updater: a release cannot reach an installed user (`tauri.conf.json:29`); a `v*` tag builds without tests, clippy or tsc (`release.yml:26`); companion plaintext HTTP on the LAN (unchanged); log file 0644 while everything else is 0600 (`applog/file.rs:37`); `secrets.rs` truncating prompt (same as Chat 5); release gate tests never run in CI; redaction blind to `"access_token": "…"` JSON shape; no `cargo audit`/`npm audit` gate; desktop CSP keeps `http: ws:` scheme sources (load-bearing for follower mode).                                                                                                                                                                                                                 |
| Yahoo                      | A− (A−)        | Season tab dead end (same as UX 1); a revoked grant never says "sign in again", Settings keeps "Connected" (`yahoo.rs:325`); loopback redirect is dead code, production always `oob` (`yahoo.rs:135`), so L5 gates the whole area; `IR+` treated as a startable slot (`engine_yahoo.rs:410`, `roster.rs:38`); load builds pick metadata from the pool alone, tick from pool + rosters (`engine_yahoo.rs:305`); `offsets_disagree` false-positives past 25 dropped rows; `opt_flag` turns Yahoo's null `is_keeper` into `Some(false)` and disables keeper inference.                                                                                                                                                                                   |
| Phone companion + follower | B (B+)         | Turning the companion off leaves every connected socket receiving frames (`server.rs:146`, publish gated on listeners not `is_running`); phone stops retrying if the host is away when it wakes (`app.js:117,244`, no catch); reconnect timer never cleared, overlapping sockets over a long draft (`app.js:222`, the follower got this right); league switch never republishes chat/season to phones (`commands_draft.rs:154`); the DOM/socket half of `app.js` is untested (`companionPage.test.ts:12` boots with no elements); chat rebuilt every second, blurs the input (`app.js:352`); `companion_status` shells out to `tailscale` on the runtime per devices event (`commands_companion.rs:33`); host and desktop share one Keychain account. |
| Logging/error reporting    | A− (A claimed) | "Verbose logging" is inert, zero `applog::debug` call sites; draft loop logs notes every tick with no dedup (`poll_loop.rs:146`); frontend errors reach the file without a stack (`ErrorBoundary.tsx:31`, `errorReport.ts:52`); redaction eats Yahoo league ids in URLs (`redact.rs:95`); nothing asserts `logged!` coverage; headless host never inits the log; no timezone anchor in diagnostics; log 0644.                                                                                                                                                                                                                                                                                                                                         |

Overall: **B+**, unchanged for the fourth card, but the shape moved. Four areas rose or held on real fixes (draft engine to A−, security to B+, advice, chat, UX, Yahoo held with every prior claim verified). Three fell for one reason each: Season and Backend share a single root cause (the season loop never got the draft loop's empty-payload and lock-time hardening), Tests fell because the audit found the suite's own safety gap (a real-Keychain state in a sweep that calls disconnect) and the reason for the load flakes, and Companion fell because the phone page's imperative half has no tests and two liveness dead ends. The pattern named at grade 5 is still the pattern: a fix applied to the instance (draft loop, follower, `yahoo_secrets`) and not the class (season loop, phone page, `secrets.rs`).

Cheapest high-impact fixes, in order:

1. `command_surface.rs:324` → `YahooState::sandboxed`; `secrets.rs` → route through `yahoo_secrets` with an `AnthropicKey` item (kills Chat 5, Security 5, Tests 1 in one lane).
2. Season loop: refuse an empty list over a non-empty one, skip the empty-week cache write, clear warnings on a good refresh, roll the week on `week` not `display_week` (Backend 1-2, Season 2 and 4).
3. Hide the Season tab for Yahoo leagues with a one-line note; `platformName` sweep across the season screen (UX 1, Yahoo 1).
4. `season_lineup.rs:127` apply the injury tag to the current week only (Season 1).
5. Phone page: catch in `loadEverything` with `connect()` in `finally`, clear the reconnect timer, and `stop()` closes every live token (Companion 1-3), plus a jsdom boot test.
6. Vitest: `testTimeout: 20_000`, `coverage.include: src/**`, re-baseline floors; per-thread `applog` capture (Tests 2-4).
7. Release: `ci.yml` on tags too, `check:version`/`test:scripts` in CI, log file 0600, Tauri updater with a minisign key (Security 1-2, 4, 6).
8. Draft: keeper floor guard in `pre_draft` past `open_pick`, refuse a backwards `next_open_pick`, gate recommendations on `draft_over`, clear `recorded` on switch.
9. Advice: shared injury classifier, reach term at 12 picks, drop the VORP reason line, warn on unfillable roster slots.
10. Logging: debug lines at the tick boundary, note dedup in the draft loop, component stack in frontend reports, league-id-safe URL redaction, `logged!` sweep assertion.
11. Relabel the thirteen Yahoo fixtures `HandWritten` until L5 captures real responses; `IR+` handling; revoked-grant message.
12. Chat: assert cache reads across two ticks and move the board into a mid-conversation system message; align the shared timeout with the client and record spend before returning; sanitise names into the prompt.

### Grade-6 fix batch (2026-09-05) — ten Fable lanes, one commit

All twelve items of the cheapest-first list. Verified on the combined tree: Rust 1218 tests, frontend 811 tests in 66 files, Playwright 36/36 (against the dev server locally; CI now runs it against the production bundle), clippy, tsc, eslint, prettier, line cap and CSS checks clean.

- **Secrets / test safety:** `secrets.rs` is a thin layer over the shared store with `Item::AnthropicKey` (same account name, so stored keys keep reading); the command sweep uses `YahooState::sandboxed` and a self-scan keeps it that way; `logged!` coverage asserted by `tests/logged_coverage.rs`.
- **Season:** empty matchups/rosters/scores never replace a non-empty set and fail the source; empty settled weeks never cached; injury tags apply to the current week only in odds and Trends; IR slots excluded; `current_week` reads `week` not `display_week`; incomplete-players warning clears; `lineup_locked` computed in Rust (empty and bye slots stay open) and read by the screen; bye teams not double-counted; waiver pool valued on rest-of-season; Sleeper-only guard in `commands_season`.
- **UX:** Season button disabled for Yahoo with "Season view is Sleeper-only for now"; `platformName` across the season screen; keyed ErrorBoundaries with a recovery test; `remoteFetcher` 10 s timeout and Leave host reachable from every follower dead end; username-only save from the settings row; Back names the league you left; Diagnostics/ErrorBoundary clipboard states; `role="alert"` sweep; em-dashes out of UI copy.
- **Companion:** `stop()` closes every socket (1001, tokens kept) and aborts its tasks; `publish*` no-op when not running; league switch republishes both threads and clears the week on phones; phone page: `loadEverything` catches and always connects, one socket and one timer at a time, chat POST failure keeps the text, chat block only re-parented on tab change; new jsdom boot test (4 cases); Tailscale lookup cached in the hub; lockout maps pruned; dead store test replaced.
- **Release / tests infra:** `release.yml` runs the CI verify job via `workflow_call` before building; `check:version`, `test:scripts`, `cargo audit` and `npm audit` (allow-listed by advisory id with reasons) in CI; `tauri-plugin-updater` registered with `createUpdaterArtifacts`, endpoint on the GitHub release `latest.json`, public key in `tauri.conf.json`, artifacts gated on the `TAURI_SIGNING_PRIVATE_KEY` secret being present; vitest `testTimeout` 20 s and `coverage.include`; Playwright against `vite preview` in CI.
- **Draft / backend:** keeper floor applies in `pre_draft`; a partial `/picks` answer that rewinds the clock is a refused tick; `view_now` checks the draft id; `final_owner` consumes each trade link once; no recommendations after the last pick; `recorded` cleared on league switch; note-only calls one attempt at 3 s; slow-tick and note dedup watches; unparseable config kept as `.broken-<ts>` with `.bak` fallback; view built off both locks in `spawn_blocking`; headshot temps swept.
- **Advice:** shared `injury_class.rs` (Suspended = missing weeks); reach term at one round; VORP line off the reasons; upside two-directional; `weeks_left` real; IDP slots warned and excluded from open starters; any `IR*` slot non-starting. IDP players on the board not done (projections feed and scoring have no IDP).
- **Logging:** debug lines at both tick boundaries, socket open/close, pairing, chat request/answer; per-thread capture; log file 0600; single kept handle; Yahoo league ids survive URL redaction; JSON token shapes redacted; frontend reports carry the first two frames; local-time anchor in diagnostics; headless host logs.
- **Yahoo:** revoked/dead grant clears tokens and says "Yahoo signed you out. Connect again in Settings."; `IR+`/`IR2` non-draftable; load and tick build pick metadata from the same map; unreadable rows counted in resume; all-null `is_keeper` is `None`; nonce from the CSPRNG; every fixture relabelled `HandWritten`; `REDIRECT_FLOW` constant with the loopback path wired and tested (still `Oob` until L5).
- **Chat:** streaming (SSE parser, `MAX_TOKENS` 64000, partial usage on failure); stable prefix = league/scoring/roster/rules only, board as a trailing system message, byte-identical across ticks (tested); spend recorded before return and on abandoned turns; shared timeout above the client's; names sanitised; `check_screen` first; refusal category shown; error bodies logged redacted and short.

Done 2026-09-07: secret set, first signed release is `v0.3.1` (see Grade 7 note below).

### Grade-7 prep batch (2026-09-06) — three Fable lanes, commit `0a121c5`, release `v0.3.0` at `2534563` (first tag at `620a5b6` failed its gate: the companion browser spec 404ed `pwa.js`; fixed, tag moved, dmg published)

- **Updater row:** Settings → "Check for updates" (`commands_update.rs`, `updateRow.ts` state machine, `useUpdateRow.ts`); plugin errors mapped to plain sentences; hidden where the desktop api is absent (follower, web preview). Idle value is "Check" because the Version row sits directly under it.
- **Companion HTTPS:** when `tailscale cert` mints for the MagicDNS name, a second listener on port+1 serves the same router over TLS (`companion/tls.rs`, `tls_x509.rs`, `tls_serve.rs`; tokio-rustls on ring, hyper 1); the https origin joins CSP and the WebSocket origin set; the tailnet URL prefers https. Phone page: manifest, pass-through service worker, `pwa.js` wake lock held while a draft is live. L10 added.
- **Leftovers:** `RosterRules::counts_as_open_starter` at every open-starter site; season screen passes `platform` to every child; ErrorBoundary hands the component stack to the reporter; apiRemote copy without em-dashes; loopback Yahoo flow hides the code field; headless `companion_host` keeps pairings in `companion-devices-headless`.
- Verified on the combined tree: Rust 1249 tests, frontend 840 tests in 68 files, clippy/tsc/eslint/prettier/loc/css clean, `cargo audit` clean. Version bumped to 0.3.0 in all four manifests and tagged.

## Grade 7 (2026-09-06, after the grade-6 fix batch and the grade-7 prep batch, at `v0.3.0`) — eleven read-only Fable auditors

Fresh numbers at `620a5b6`: Rust 1249 tests, frontend 840 tests in 68 files, Playwright 36 in 3 files; CI and End-to-end green on `0a121c5`. Rust coverage was not re-measured for this card.

Prior claims: every grade-6 fix and every grade-7 prep change was re-verified by a fresh auditor in code and by a test that fails without it. Four did not hold as written: the em-dash sweep covered two files and left roughly forty strings; the half-hour season injury refresh is served from the 24 h players cache and never fetches; "Refresh rolls the week" has no desktop caller; and `tls.rs:24` says the headless host runs TLS off while `companion_host.rs:147` uses the default Tailscale source. Two lack a failing-without test: empty settled week never cached (`season_engine.rs:160`) and `stop()` sending 1001 on a live socket.

| Area                       | Grade (prev) | Top verified findings                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Draft screen/engine        | A− (A−)      | A failed `/users` call silently makes the commissioner's seat "mine" with a green frame and chime (`engine.rs:270`, `view.rs:80-96`); the empty/hole refusal guards have no exit, so a persistent hole freezes the board for the rest of the draft (`poll_loop.rs:106`, `commands_draft.rs:264`); Refresh projections re-derives the keeper floor from that instant's `/picks` (`commands_draft.rs:347`); `refresh_picks` writes to disk under the `loaded` lock (`:289`); vacuous 500 ms sleep test (`switch_tests.rs:105`); draft buttons live after the draft ends (`Board.tsx:264`); `switchLeague` turns live sync back on (`draftSession.ts:294`). |
| Drafting-advice quality    | A− (B+)      | Yahoo injury codes (Q/O/D/IR-R/PUP-R/NFI-R) never reach the shared classifier, so every "Q" is docked six all night and PUP-R stays on the card (`yahoo_map.rs:412`, `injury_class.rs:29`); camp PUP priced as season-ending pre-draft (`injury_class.rs:31`); K/DEF charged the bye clash the design says is meaningless (`recommend_strategy.rs:28`); "best available (fallback)" and "depth pick — 0 RB already rostered" shown verbatim (`recommend_score.rs:54,225`); second QB in a shallow superflex pool docked as a backup (`recommend_demand.rs:91`).                                                                                          |
| Season                     | B+ (B+)      | The Sunday-morning injury refresh is a no-op: `players(false)` returns the 24 h cache (`season_refresh.rs:139`, `projections.rs:37`), so a starter ruled Out stays in the optimal lineup all day; the poller's `AnalysisCache` is never invalidated when a command replaces `state.season`, re-emitting stale standings and waivers for up to 20 ticks (`season_loop.rs:337`); a failed week rollover is silent on both paths (`season_loop.rs:216,352`); `refresh_season` has no desktop caller (`api.ts:211`); live chips fall back to projection for a started player missing from `players_points` (`season_view_live.rs:59`).                       |
| Chat                       | A− (B+)      | No retry on 429/529/5xx and 529 reads "unknown status code" (`chat.rs:370,457`); closing the panel or switching screen mid-answer loses the question and the spend (`App.tsx:432`, `Chat.tsx:203`); no cancel for a ten-minute answer and no partial text (`chat_client.rs:22`); desktop spend counter does not move for phone-asked turns (`Chat.tsx:138`); phone question accepted while the desktop holds the claim (`routes_chat.rs:181`); key visible in `ps` as hex argv (`yahoo_secrets.rs:160`).                                                                                                                                                 |
| Backend/data               | A− (B+)      | Any CDN non-2xx writes a three-day "no photo" miss (`headshots.rs:203`); `save_config` runs write+fsync+rename under the `config` mutex at nine sites, per chat turn (`commands_chat.rs:371`); season load still does sync disk work on the runtime (`season_engine.rs:144,166`, `engine_assemble.rs:54`); cache write failures never logged in season (`engine.rs:150`); one season source failing for an hour is green to the poller (`season_sources.rs:118`); `record_history` unlocked read-modify-write (`season_history.rs:183`); no `SlowTickWatch` on the season loop (`poller.rs:118`).                                                        |
| Tests                      | B+ (B)       | Keychain safety guarded per file: `command_flows.rs:89` builds `YahooState::default()` and `engine/config.rs:160` migrates an Anthropic key into the real Keychain with no store parameter, so a scratch-config test can overwrite the user's key; `e2e-browser/companion.spec.ts:24` CSP drifted and its fake server 404s `pwa.js`, manifest and worker; timing flakes (`https_tests.rs:99` 50 ms sleep, `yahoo_auth_wire.rs:370`), Playwright `retries: 2` in CI; `COVERAGE_AFTER_PLACEHOLDER` never filled (`vitest.config.ts:41`); headless account order untested (`companion_host.rs:145`); CI builds the bundle twice.                            |
| Integration/UX             | B+ (B+)      | Em-dashes remain in about forty user-visible strings across `ClockBanner`, `Board`, `Chat*`, `TrendsTab`, `Panels`, `commands_yahoo.rs`, `app.js`; follower POSTs have no timeout and `SharedChat.tsx:88` clears the draft before awaiting (`apiRemote.ts:344,359`, `companion.ts:128`); every settings row is `menuitemcheckbox` and rows are keyed by label so the updater row drops focus when its label changes (`Header.tsx:431,437`); `PREVIEW_VERSION` stale at 0.2.0 (`appVersion.ts:8`); Diagnostics note and launch error not announced; username Save with an empty field reloads the league (`SetupScreens.tsx:57`).                         |
| Release/ops/security       | A− (B+)      | `TAURI_SIGNING_PRIVATE_KEY` is not set, so v0.3.0 ships a dmg only and "Check for updates" answers "No release feed yet" everywhere; desktop CSP `connect-src` lacks `https: wss:`, so a follower cannot join the https tailnet URL Settings now shows first (`tauri.conf.json:24`); a re-mint failure inside the 14-day window drops HTTPS though the cert is still valid (`tls.rs:104`); HTTPS loss is debug-only (`tls.rs:146,155,214`); https port not stable across launches, stranding an installed PWA (`tls_serve.rs:32`); headless host mints real certs against its own docstring (`tls.rs:24`).                                               |
| Yahoo                      | A− (A−)      | Keychain write every three-second tick during a Yahoo draft (`commands_yahoo.rs:80`, `secrets.rs:140`); sign-out and loopback completion never reach the Settings row (`yahoo.ts:30`, `App.tsx:126`); loopback listener cannot be cancelled, port bound 300 s (`loopback.rs:35`); single `accept` on the loopback port loses the real redirect to a speculative connection (`yahoo_redirect.rs:68`); `decode` can panic on a stray `%aé` (`:162`); empty stored refresh token is a permanent error, not a sign-out (`yahoo_oauth.rs:410,346`).                                                                                                           |
| Phone companion + follower | B+ (B)       | Phone chat question lost on any non-network failure (`app.js:256,274`); HTTPS decided once at start, a Mac joining its tailnet later shows an http QR until toggled (`server.rs:159,187`); follower `sharedChatSend/Reset/image` bare `fetch` (`apiRemote.ts:254,343,358`); iOS ignores the SVG touch icon and an installed PWA re-pairs as "iPhone 2" (`index.html:15`); wake lock re-requested every second while hidden (`pwa.js:44`, `app.js:450`); cert expiry checked only at start (`tls.rs:133`).                                                                                                                                                |
| Logging/error reporting    | A− (A−)      | Follower's own crashes leave no line: `logFrontendError` is a no-op and `log_path` null in follower mode (`apiRemote.ts:434,413`); every reason HTTPS did not come up is debug-only (`tls.rs`, `tls_serve.rs:35,39`); updater cause lost before `logged!` (`commands_update.rs:117`); level applied before the config save can fail (`commands_diag.rs:112`); pairing lockout debug-only (`hub.rs:294`); redaction knows `Bearer` not `Basic` (`redact.rs:22`); corrupt persisted state and chat history reset silently (`persisted.ts:45`, `chatSessions.ts:122`).                                                                                      |

Overall: **A−**, up from B+ after four cards at that mark. Five areas rose (advice, chat, backend, tests, release, companion) and none fell. The remaining findings are one class: a fix applied where it was found and not at its siblings (timeouts on GETs not POSTs, em-dashes in two files, HTTPS at start not on refresh, Keychain sandbox in one test file). The two that matter most on a live day are Season 1 (stale injuries all Sunday) and Draft 1 (wrong seat on a flaky `/users`).

Cheapest high-impact fixes, in order:

1. Season: `refresh_players` reads players and projections with a 30-minute ceiling; `SeasonPollMemory` invalidates on a season epoch; failed rollover pushes a warning; wire a Refresh button or drop L7's claim (Season 1-4).
2. Draft: gate the creator-seat fallback on a real mock and warn when `/users` fails; adopt past a persistent hole after three refused ticks; carry `keeper_pick_nos` through Refresh projections; write off-lock in `refresh_picks` (Draft 1-4).
3. Desktop CSP `https: wss:`; HTTPS started from the refresh task and kept when a re-mint fails on a still-valid cert; `warn` on every HTTPS loss; stable https port; headless host `TlsSource::Off` (Security 2-6, Companion 2, 7, Logging 2).
4. One `withTimeout(fetch)` for every follower and phone request; restore the phone's chat input on any failure (UX 2, Companion 1, 3).
5. Em-dash sweep with a repo-wide grep test over string literals (UX 1, Advice 4).
6. `Engine` takes a `SecretStore`; sandboxed constructors use `FileStore`; scan `tests/**` for `YahooState::default()` (Tests 1).
7. Yahoo: skip the Keychain write when the pair is unchanged; re-ask status on dialog close and on `SIGNED_OUT`; loopback handle kept and aborted, accept loop, byte-safe decode; empty refresh token signs out (Yahoo 1-6). Yahoo codes into `injury_class`, camp PUP pre-draft (Advice 1-2).
8. Chat: two retries with backoff on 429/529/5xx; pending question saved on send and restored on remount; Cancel; spend refresh on shared-chat events; reserve before post (Chat 1-4, 6).
9. Backend: headshot miss only on 404/410; `save_config` off-thread via the `store_key_unlocked` pattern; season disk I/O through `_off_thread`; log cache failures; per-source `NoteWatch` and `SlowTickWatch` in the season poller; lock `record_history` (Backend 1-7).
10. Settings menu: stable row ids and `role="menuitem"` for actions; `PREVIEW_VERSION` from package.json; live regions in Diagnostics and launch; username Save guard (UX 3-6).
11. Logging: follower routes log calls to its local backend; updater cause in the log; save before `set_level`; lockout `warn`; `Basic` redaction; report corrupt-state resets (Logging 1, 3-7).
12. Tests: companion e2e serves `pwa.js`/manifest/worker and imports the CSP; poll-until in `https_tests` and `yahoo_auth_wire`; Playwright retries 0; fill the coverage placeholder; pass the device `Item` into `CompanionServer::new` (Tests 2-5).

Done 2026-09-07: `TAURI_SIGNING_PRIVATE_KEY` set by the user; `v0.3.1` at `a41c9e5` (run 34086367307) published the dmg, `Draft-Assistant-0.3.1-aarch64.app.tar.gz` with its `.sig`, and `latest.json`, so "Check for updates" now has a feed and 0.3.0 installs can update to it. Still open for the user: live tests L1–L10.

## Grade 7 fix batches (2026-09-07) — all twelve items closed

Three batches of parallel lanes, each committed only once the whole gate was green.

- **A `d168fa2`** — Season 1-4 (30-minute refresh ceiling, season epoch invalidating the poll memory, rollover warning, Refresh button, live zero for a started player with no points, season disk work off the runtime, per-source and slow-tick watches, `record_history` serialised). Draft 1-4 and 6-7 (seat stays unconfirmed on a failed `/users` and is retried, a hole is adopted after three refusals, keeper floor carried through Refresh, `refresh_picks` writes off the lock, actions inert once the draft ends, live sync stays off across a switch). Security 2-6 and Companion 2, 7 (CSP `https: wss:`, TLS keeper that starts on the tailnet appearing, renews in place, keeps a valid cert when a re-mint fails, stable https port, headless TLS off). UX 2 and Companion 1, 3 (one timed fetch for every follower and phone request, drafts and questions survive a failed send, iOS identity and PNG touch icon, wake lock only while visible, follower logs its own errors). UX 3-6 (menu roles, stable row ids, version from package.json, live regions, username Save guard). Logging 3-7.
- **B `03d7e9c`** — Chat 1-4 and 6 (retries with backoff, question survives a close, Cancel with partial text, spend refresh, claim reserved before post) plus the claim registry moved off a process-global. Yahoo 1-6 and Advice 1-2. Tests 1 (Engine takes its `SecretStore`, so sandboxing is by construction) and Backend 1 (headshot miss only on 404/410). Tests 2-5.
- **C `6ad9a74`** — UX 1 and Advice 4 (em-dash sweep across every tree, with `scripts/check-dashes.mjs` in `verify:fast` and nine tests of its own). Backend 2 (`save_config` off the lock at all nine call sites, ordered by sequence number; the three that report failure write before committing in memory). Advice 3 and 5.

Numbers at `6ad9a74`: Rust 51 test binaries green, frontend 915 tests in 78 files, Playwright 38. `verify:mid` clean, `cargo clippy --all-targets -D warnings` clean.
