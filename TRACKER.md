# Work tracker — 2026-08-28 (draft day, 17:00 PDT)

Status legend: `todo` · `doing` · `done <commit>` · `deferred (why)`

Updated as work lands. Items come from the grade report (`.claude/grade-report.md`),
the dogfood report (`dogfood-output/report.md`), and the in-app Claude's own list of
what it could not see.

## Requested this session

| # | Item | Status | Notes |
|---|------|--------|-------|
| 1 | B6 — chat memory + configurable model | done 86c42e3 | `chat/prompt.rs` sends the last 6 exchanges + any summary; `DRAFT_ASSISTANT_CLAUDE_MODEL` default; stub-CLI tests assert history and flags |
| 2 | D2 — unit-test command-layer pure logic | done 86c42e3 | `manual.rs` (`apply_manual_pick` / `undo_manual_pick`, 5 tests, plus a new not-on-the-board guard); `sleeper::extract_id` (5 tests) |
| 3 | Chat: model selector (Opus / Sonnet / Fable) | done 86c42e3 | Settings fold in the panel; remembered in localStorage |
| 4 | Chat: speed selector (fast mode) | done 86c42e3 | `--settings '{"fastMode":true}'`; the account reports `extra_usage_disabled`, so the panel notes once "answered at standard speed" — enable extra usage to get it |
| 5 | Chat: thinking/effort selector | done 86c42e3 | `--effort low/medium/high/xhigh/max`, Default = CLI default |
| 6 | Chat: full app context (whole board, not top 40) | done 86c42e3 | all 419 players as a pipe table (~10k tokens vs 34k for JSON); state JSON keeps rosters, recent picks, baselines |
| 7 | Chat: web-search toggle, default off | done 86c42e3 | `--tools WebSearch` when on, `--tools ""` when off; system prompt tells the model when it may search |
| 8 | Chat: new-session button | done 86c42e3 | **New chat** in the panel header |
| 9 | Chat: token usage display | done 86c42e3 | usage line: context tokens, seconds, model, question count, session cost, web searches |
| 10 | Chat: compaction option with "takes a minute or two" warning | done 86c42e3 | **Compact** (enabled after 2 questions); warning in the tooltip and the pending line; summary replaces the thread as history |
| 11 | Board: click column headers to sort (Bye, Pos, Pts, VORP, Tier, ADP, Surv) | done 86c42e3 | every header sorts; second click flips; `#` restores value order; blanks last; `aria-sort` set; unit + E2E tests |
| 12 | Why `weekly_2026.json could not be cached (replace …: No such file)` | done 86c42e3 | Two loads overlapped (React dev StrictMode double-mount at launch) and shared one `weekly_2026.json.tmp`; the loser's rename found nothing. Fix: per-write tmp names (`store.rs`, 8-thread race test) + launch effect guarded to run once (`App.tsx`). Harmless in effect: the data was in memory and used. |
| 13 | Claude's wish list: full board / weekly data / live picks | done / deferred | full board: done (item 6); weekly cache: done (item 12); schedule/SoS/playoff weeks: deferred (schema change); live picks: already flow in |
| 14 | Dogfood ISSUE-001 — confirm modal ignores Escape, no focus | done 86c42e3 | native `<dialog>` (`ConfirmDialog.tsx`): focus lands on Confirm, Escape/backdrop cancel, focus returns to the row; E2E test |
| 15 | Dogfood ISSUE-002 — chat Escape lost after focus leaves | done 295d2c9 | |
| 16 | Dogfood ISSUE-003 — chat drawer covers header + decision columns | done 86c42e3 | panel is a sticky flex column beside the page; board scrolls horizontally if needed; E2E asserts nothing actionable sits under it |
| 17 | Dogfood ISSUE-004 — sticky alert covers a board row | done 86c42e3 | toast anchored top-right under the header |
| 18 | Dogfood ISSUE-005 — header wraps at 1000 px | done 86c42e3 | league name ellipsises, buttons wrap as whole units, pill never breaks mid-word |
| 19 | Dogfood ISSUE-006 — preseason QUESTIONABLE penalised in safe mode | done 86c42e3 | `recommend::serious_injury` (out/ir/pup/sus/doubtful/na/cov) gates the −15; badge muted for Questionable with a tooltip; tests both sides |
| 20 | Dogfood ISSUE-007 — preview greets with red alert | done 86c42e3 | `api.preview` skips live sync and shows a neutral banner; E2E asserts no alert on load |
| 21 | Write-up: crib sheet vs app, what AI adds, app vs Sleeper web | done | see the section at the bottom |
| 22 | Demo / replay mode (asked 05:25) | done 86c42e3 | `scripts/replay-sleeper.mjs` replays any completed draft as live; `DRAFT_ASSISTANT_SLEEPER_BASE` override in `sleeper.rs` (D1's seam); browser preview `?replay=<url>` polls dumps; README "Demo / replay mode" |
| 23 | Live test of the app via Chrome + mock draft | done differently | your logged-in Chrome is unreachable from this harness (no debug port; Chrome profile folder blocked by macOS). Tested instead against a live replay of the league's 2025 draft with you at slot 2 — 13 scenarios, all pass: `dogfood-output/replay/report.md` |
| 24 | R-1 sticky alert covered wrapped header buttons (regression from item 17) | done 86c42e3 | found by the replay test; failures are now an in-flow bar under the header, toasts are click-through |
| 25 | R-2 recommendation card shown after draft complete | done 86c42e3 | `build_view` returns no recommendations once `draft_over` |
| 26 | Bug hunt (asked 06:00) — 12 validated items H1–H12 | done, all 12 | each item: failing test → fix → passing → own commit. H1 3c83f8c · H2 7115c33 · H8 a6f5e98 · H9 e5d53c7 · H6 693383b · H7 ec52755 · H3 5cefda4 + 6901d95 · H4 d76d6a5 · H5 d2d8359 · H10 9f9dccb · H11 a257c0f · H12 c6c9731. Results table in `dogfood-output/bug-hunt-2026-08-28.md` |
| 27 | Replay server stamps `last_picked` at real release time | done | so the new pick clock counts down in replay mode like a live draft |
| 29 | Dogfood pass, all areas (asked 07:00) | done, 13 issues | `dogfood-output/full-2026-08-28/report.md` — 1 high (live-sync false success), 7 medium (false success toasts ×2, silent launch failure, raw parser error, clock live-region chatter, board clipped with chat open), 5 low. Verified-working table covers sorting/search/dialog/clock/live sync/chat. Desktop-only paths untestable (WKWebView has no CDP) |
| 30 | Fix all 13 dogfood issues (asked 07:05) | done, all 13 | test-first per item: 001–005+011 `5c7d672` · 008 `ec1ff0a` · 006/010/012 `6fffbb4` · 009 `d0c54eb` · 007 `6232c9d` · 013 `fdbdbfa`. Results table in `dogfood-output/full-2026-08-28/report.md`, each verified in the running app |
| 31 | **Keeper league support** (found while regenerating the fixture) | done | the 2026 draft carries 23 keepers at picks 11–177 while still `pre_draft`: the clock read "Pick 24" before anyone picked, manual picks were silently discarded, and recent picks led with round-13 keepers. `aedfa8f` + `467ba44`, covered by `src-tauri/tests/keepers.rs` |
| 32 | Dogfood pass 2 (asked 08:20) | done, 4 issues, all fixed | `dogfood-output/pass2-2026-08-28/report.md` — 1 high (keepers made the app claim the draft was live 10 h early), 3 low (tier badge colours, dead `injured` class, board clipped at 1100px). All fixed test-first in `f5c3a1a`. Clean: error boundary, corrupt settings, interaction races, 5-min soak (flat 16 MB, 0 errors), tonight's real state rendered end to end |
| 33 | Pre-draft shortlist from the grade report (asked 09:15) | done | B1 large-download timeouts `f1339b0` · C5 `/` to search + C2 "Show all" `82068f8` · H1 pre-draft runbook in the README. Re-dumped tonight's league after the changes: byte-identical state, no warnings |
| 34 | Push + PR so CI finally runs (grade items D3/I1) | done, green | [PR #1](https://github.com/jtn0123/draft-assistant/pull/1). It failed three times first, each a real defect only CI could find: start-time tests asserted a Pacific wall-clock string (`424a09f`), the runner's Rust 1.98 flagged three lints my 1.88 does not — now pinned via `rust-toolchain.toml` (`ca3dd84`) — and two App tests rendering 200 board rows blew the 5 s timeout on a loaded runner (`bb48e47`). Run 33188132961 green |
| 35 | Pre-draft data refresh + full functionality check (asked 09:25) | done | All three app caches re-downloaded 09:27 (one injury tag changed in six hours: Cameron Dicker, a kicker). Everything exercised on tonight's real data — banner, keepers, board, dialog, live sync, a real $0.24 Claude call, `verify` exit 0. `dogfood-output/final-check-2026-08-28/report.md`. Note: projections TTL is 6 h, so a relaunch after 15:27 re-downloads ~20 MB |
| 36 | AI integration: fix all six gaps from the assessment (asked 10:39) | done | **Streaming** — `stream-json` parsed line by line (`chat/stream.rs`), text pushed over a Tauri `Channel`, the panel renders the answer as it is written with a caret, Cancel keeps what was written. **Markdown** — `chatMarkdown.tsx` renders bold/italic/code, bullet and numbered lists, headings; no HTML ever interpreted. **Cost** — the user's own MCP servers were adding ~16k tokens of tool schemas to every call; `--strict-mcp-config` drops them (42.9k → 21k context tokens on the real board); a session budget ($5 default, 0 = none) stops asking when reached. **Staleness** — every answer is stamped `as of pick N`, amber with "k picks since" once picks land. **Injection** — state and board fenced in `<draft_state>`/`<board>` tags the system prompt names as data; names stripped of pipes/newlines and clipped. **Proactive** — "Ask when I'm on the clock" fires the one question that matters the moment it is the user's turn, once per pick, and opens the panel. Real three-question session recorded through the new path with `dump_state --ask … --chat-out` (5.7 s / 7.7 s / 19.5 s, $0.67) and screenshotted via `?chat=` replay: `dogfood-output/ai-session-2026-08-28/`. 24 new tests (9 Rust, 11 Vitest, 1 E2E + Markdown suite) |
| 37 | Heavy test building toward 80 % coverage (asked 11:50) | done | Measured first: Rust **72.1 %** lines, frontend **86.2 %**. Extracted the Tauri command bodies and the poll loop into a Tauri-free `app::AppCore` (grade A1/D1 — `desktop.rs` is now 150 lines of glue), built a stub Sleeper server + stub Claude CLI shared by the integration tests (`tests/support/`), and added 25 Rust tests (engine fetch/cache/fallback matrix, every command incl. manual-pick rollback, the poll state machine, `dump_state` run as a real process) plus 8 for `api.ts` (Tauri channel bridge, replay polling, recording playback). Result: Rust **92.0 %** lines (`desktop.rs`/`main.rs` are the only zeros), frontend **93.4 %**, `api.ts` 100 %. `bun run coverage` reproduces both numbers. `dump_state` gained `DRAFT_ASSISTANT_DATA_DIR` so tests never touch the real CLI cache |
| 28 | Full `bun run verify` after the bug-hunt fixes | done, exit 0 | Rust 98 tests (69 lib + 29 integration), Vitest 41, Playwright 11, clippy/eslint/LOC clean; replay preview screenshot `dogfood-output/replay/13-bug-hunt-fixes-pick-58.png` |

## Data the chat could not see (from the in-app Claude)

- **Full board** — was capped at 40 of 419 (`chat.rs AVAILABLE_LIMIT`). → item 6.
- **Weekly projections uncached** — the cache write raced, not the fetch; the data
  was in memory and used for the board. → item 12. Schedule/SoS and playoff-week
  (15–17) points are not in `DraftView` at all; adding them is a schema change
  (`1.2` → `1.3`) — deferred until after the draft.
- **Live pick context** — `recent_picks` and `position_run` fill in as picks land;
  nothing to change, the chat already receives them on every question.

## Deferred (post-draft)

- Playoff-week / strength-of-schedule fields on `BoardPlayer` (schema bump).
- Remaining grade-report items: D1, B1, B5, G1, C1 (covered by item 14), I2 CI run.

## Verification (2026-08-28 ~04:10)

`bun run verify`: LOC cap ok · cargo fmt ok · tsc ok · vite build ok · Vitest 31/31 ·
Rust 54 lib + integration suites ok · Playwright 11/11 · eslint + clippy clean.
Live smoke test (04:20): today's league via `dump_state` → the new prompt shape
(5.4k chars of state JSON + 20k chars of board table, 419 rows) → real `claude`
CLI with the app's flags. Opus answered a QB2/DEF plan naming Purdy (ADP 122),
Rams / Philadelphia / Denver / Detroit DEF — players far outside the old top-40
slice — in 36 s, ~42k context tokens, $0.50 as the CLI prices it. Timeouts were
raised to 90 s (150 s with web search) on the strength of that measurement.

Replay run (05:35–06:05): `bun run verify` re-run after R-1/R-2 — Vitest 33/33,
Rust 56 + suites, Playwright 11/11, lint/clippy/fmt clean.

To use your real logged-in Chrome for a Sleeper mock draft next time, either:
`open -na "Google Chrome" --args --user-data-dir=$HOME/chrome-debug --remote-debugging-port=9222`
(log the testing account in there once), or save its credentials with
`agent-browser auth save sleeper --url https://sleeper.com/login --username <name> --password-stdin`.
Chrome ≥ 136 refuses remote debugging on the default profile, so the plain
"already logged in" window cannot be attached to.

Committed through b1d785c on `t3code/review-prior-grade-report` (applied to the live app 21:24, verify exit 0). Not pushed.

## Why this beats a crib sheet / the Sleeper web UI (summary)

- **A crib sheet is frozen at print time.** It cannot know your league's exact
  scoring (6-pt pass TD, PPR, DEF buckets), cannot re-rank as players leave the
  board, and cannot tell you "who survives to pick 55". The app's board is
  scored from the league's own `scoring_settings`, VORP is measured against
  replacement demand for a 14-team roster shape (**but see row 56** — that
  demand is derived from the projections it then scores, and tonight's room
  disagreed with it), and survival odds are recomputed from the live pick
  count every 3 s (**row 57** — from ADP alone, not from what the room needs). A crib sheet is still worth
  having as a sanity check for a name the model or the projections mis-price.
- **Sleeper's own UI shows ADP and default ranks,** not value over
  replacement, not tier scarcity, not your open starters, not who is likely
  gone before you pick again. It also cannot be asked a question.
- **What the AI actually adds** is judgement over the numbers, not the
  numbers: trade-offs ("RB2 now or the last tier-2 TE?"), roster construction
  across the next three picks, why a flagged player matters (with web search
  on), and explaining the app's own recommendation in plain words. It is fed
  the same state the board renders, so it cannot invent a player; the
  deterministic recommendation is the floor, the chat is the second opinion.
| 38 | Saved chat sessions + new sessions (asked 12:55) | done | One JSON file per session under the data dir (`chats/<draft_id>/<id>.json`, `chat/session.rs`, tmp+rename, ids sanitised to one path segment), three commands (`save/list/load_chat_session`) forwarded through `AppCore`, a **Sessions** row in the panel that lists the draft's sessions and reopens one in place, autosave after every answer/compaction/cancel, restore of the latest session when a draft opens, and **New chat** starting a separate file. Browser preview runs the same over `localStorage` (`sessionStore.ts`). Tests: 4 Rust (`tests/chat_sessions.rs`) + 2 unit, 5 Vitest (`ChatSessions.test.tsx`) + 2 (`sessionStore.test.ts`), 1 Playwright (reload restores, New chat separates). The e2e caught a real bug: a "restored already" ref made StrictMode's double mount cancel the restore for good. |
| 39 | Dev watcher reloads the live window on test output (I2, observed 12:37) | done | `vite.config.ts` `watch.ignored` now covers `coverage/`, `playwright-report/`, `test-results/`, `dogfood-output/` and `public/ai-*.json`. Takes effect on the next `tauri dev` launch. |
| 40 | `verify` fails after a coverage run (I1) | done | `eslint.config.js` ignores `coverage`, `playwright-report`, `test-results`, `dogfood-output`; `scripts/check-loc.mjs` skips `coverage/` too (it had the same blind spot — three 500+-line HTML reports). |
| 41 | Keeper tags, keyed on position not the flag (B2) | done | `Pick.is_keeper` deserialised as a hint; `view::keeper_pick_nos` judges keepers by "in the book at or beyond the next open pick"; `LoadedLeague.keeper_pick_nos` remembers them across polls so a keeper stays one after the draft passes its slot; `RosterEntry.is_keeper` rendered as a **keeper** tag; keepers excluded from recent picks and runs whether or not the draft has reached them. Live feed at 12:41: 27 keepers, 24 flagged. Tests: 2 in `keepers.rs`, 1 in `app_core.rs`, 1 Vitest. |
| 42 | Weekly-projection path untested (D2) | done | `tests/board_fixture.rs`: bye inference (full slate, a stray traded-player row, a team with no weekly rows), per-game bonus expectation only when the league pays it, and the "no scored players" degradation. `board.rs` 84.6 % → 94.6 % lines. |
| 43 | Honest frontend coverage (D1) | done | `vitest.config.ts` `coverage.include` names every source file; `api.ts` appears (94.5 %), the headline is 91.4 % over all files rather than 93 % over a subset. `Markdown.tsx` still does not appear in the v8 table despite the include — noted, not chased. |
| 44 | Drive the real desktop window and test it (asked 15:50) | done | Accessibility + screen recording granted, so the real WKWebView window was driven with CGEvent clicks and screenshotted. Verified live: keeper tags on the roster, session restore on open, a question asked **through the panel** (streaming Channel → answer → autosave), New chat writing a second file, the Sessions switcher, `/` search, position filter, ADP sort, Export state, and manual pick → Undo. Found and fixed two real bugs (row 45) plus a cosmetic one: a reopened session's label read "not saved yet". Screenshots in `/tmp/app-test/`. |
| 45 | **Manual picks were silently deleted 3 s after being made** (found 16:05 by driving the app) | done | `engine::reconcile_manual_picks` still used the old `pick_no > api_max` rule that `view::merged_picks` was fixed away from. This league's feed opens with keepers up to pick **195**, so *every* manual pick made tonight was retired by the next poll — the fallback for a lagging Sleeper API was completely broken, and it failed silently. Second half: the poll only emits a view when the *feed* fingerprint changes, so the UI kept rendering the phantom pick and **Undo** answered "no manual picks to undo". Fixed both (keyed on pick number + player, and `manual_changed` forces an emit); 1 unit + 2 integration regression tests; re-verified in the real window — the pick now survives 4+ polls and Undo restores pick 1 cleanly. |
| 46 | Escape on the confirm dialog closed the chat panel (found 16:18 in the running app) | done | `Chat.tsx` guarded on an open `<dialog>` but listened in the bubble phase; the dialog removes itself during the same keypress and React flushes that first, so the guard saw nothing. Moved to the capture phase. The old test passed because it never let the dialog close itself — the new one does and fails without the fix. Also landed grade item **C5**: the folded settings header now names auto-ask and the budget. |
| 47 | Write a log the user can find (grade item **B1**, asked 16:20) | done | New dependency-free, Tauri-free `log.rs` in the domain crate (so `dump_state`, the tests and the app all log the same way): stderr always, plus `~/Library/Logs/com.justin.draft-assistant/draft-assistant.log`, rolling at 4 MB with one backup. Wired through `AppCore` (loads, refreshes, manual picks/undos with rollback, poll failures and recoveries, every feed change with `seq`/picks/current pick, chat with duration/tokens/cost), `store.rs` (every cache read and write — the "why did it re-download 20 MB" question), and `desktop.rs` (startup, plus the two failures that previously vanished: a rejected `emit` and a closed chat `Channel`). 3 unit tests; verified live — the file shows startup, cache ages, the poll loop, a manual pick and its undo. |
| 48 | **The pick-1 clock would have opened at 0:00** (found 16:27, headless) | done | The live draft has `pick_timer: 90` and `last_picked` stamped at **10:35 this morning** — when the 25 keepers were entered, six hours before the 17:00 start. `view::build_view` preferred `last_picked` over `start_time`, so the moment the draft flipped to `drafting` the CLOCK cell would have read 0:00 for pick 1 and stayed there until someone actually picked. Now the clock runs from whichever is later; a real pick still wins once one lands. Regression test in `view_feed.rs` built from the real timestamps. |
| 49 | Visual snake: who is picking, their clock, and how many are in front of you (asked 16:31) | done | `SnakeStrip.tsx` under the clock banner: a chip per pick from the one on the clock through **your** next one, so "how many ahead of me" is counted rather than read. On-the-clock chip is ringed and carries the live countdown (turning red under 10 s); your chip reads **YOU** in accent; keeper picks are struck through because they sit in the order but nobody waits on them; a long wait collapses to a dashed `+N` so your chip is always the endpoint. Reverses on even rounds. If the computed slot disagrees with the backend's `on_clock_slot` (a draft type the frontend does not model) it renders nothing rather than naming the wrong manager. Countdown extracted to `useCountdown` and shared with the banner so the two cannot drift by a tick. 6 Vitest tests; CSS split to `snake.css` for the 500-line cap. |
| 50 | Pick numbering toggle: overall vs round.pick (asked 17:38) | done, uncommitted | `pickFormat.ts` — one `PickStyleContext` drives the clock banner, Your picks, the snake chips and their titles, Won't last, Recent picks, the confirm dialog and the window title. Header button labelled with the live pick in the current notation (`#55` ↔ `4.13`). Round.pick is the pick's position in the round (Sleeper's convention), not the drafting slot. Persisted in localStorage, default overall. 8 tests. |
| 51 | Chat links rendered as giant URLs (asked 17:45) | done, uncommitted | The renderer had no link support at all. `[title](url)` shows the title; a bare URL shows its hostname with the full address in the tooltip; trailing sentence punctuation stays outside the link; http(s) only. Clicks go to the real browser via `openExternal.ts` (opener plugin / `window.open`), never navigating the webview the draft lives in. 5 tests. |
| 52 | Recommendation scored drop-off, not raw VORP (asked 17:25, live 17:34) | done, uncommitted | `dropoff = vorp − E[best at position at your next pick]` + `0.12·vorp`; flat ±6 survival bonus removed; tier bonus halved. Exposed a pre-existing bug: backup QB/TE penalty fired in superflex. Replayed pick 30 → Montgomery (RB) instead of a WR. |
| 53 | DEF too early · two same-team WRs · no RB depth (asked 17:47, live 17:57) | done, uncommitted | DEF/K window narrowed to the last **two** rounds; −10 tilt for a second player from one offence at one position, with the reason shown; four FLEX slots credited every position equally so receivers won every tiebreak — flex credit now discounted by how many of the position you hold, and the "depth pick" penalty scales the same way as a discount on the gain (a flat one was a positional ban and lost a 60-VORP WR to an 8-VORP RB — caught by test). Both ends of the drop-off floored at replacement: a barren position had manufactured the biggest cliff on the board and recommended a −2 VORP RB over an 18-VORP WR. Tests moved to `tests/recommend.rs` for the 500-line cap; 6 new. |
| 54 | Thin-at-RB / last-of-tier bonuses carried a −24 VORP RB to the top (found 18:05 in the prompt dump) | done b1d785c | Fix built and tested in `/tmp/da-fix` (gate both bonuses on `vorp > 0`; drop the flat "already N rostered" penalty that double-counted the crowding discount — 171 Rust tests, clippy clean). Held because the −24 is measured against an inflated RB baseline (row 56); against the real waiver level Rodriguez is ~+20, so the patch would flip the pick the wrong way. Apply only together with row 56. |

## Post-draft — ranked from tonight's evidence (2026-08-28, written at pick 145)

Everything the draft itself taught us, ordered by how much each one distorted
what the app told the user. Rows 55–57 are the reason the app was wrong about
the *room*; the rest are the reasons it was expensive or awkward.

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 55 | **Traded picks are invisible** — read `/draft/{id}/traded_picks` and `slot_to_roster_id`, assign picks by `picked_by` | done b1d785c — new `trades.rs` (`PickOwnership`: slot → roster → traded owner → slot), `build_rosters` keyed on `picked_by` with the pick-number owner as fallback for manual picks, `DraftStatus.traded_pick_slots` (schema 1.3 → 1.4) so the strip draws the real manager, trade fetch best-effort with a warning. Verified on the live league: 40 trades, 38 overrides, all 13 rosters now match Sleeper by `picked_by`. 4 unit + 4 integration + 3 Vitest. `pick helpers` moved to `picks.rs` for the LOC cap | 40 traded picks in this draft; **36 of 150 picks were made by someone other than the slot's owner**. `build_rosters` groups by `draft_slot`, so 9 of 13 opponent rosters are wrong (fisher23 shown RB4 WR3 QB1, really RB7 WR6 QB3; skoLinNH shown RB2, really **RB0**). The AI was fed those rosters when told to reason about opponents, and the ChrisWitz prediction given in chat was half wrong. The user's own picks were unaffected (roster 13 has no trades), which is why nothing looked off from slot 2. `picked_by` is already parsed and never used; the future-pick ownership needs the endpoint plus a roster→slot map. Also mis-attributed two open chips on the strip (153, 164). |
| 56 | **Replacement baseline is circular** — `valuation.rs` allocates FLEX demand to whichever position projects best, then scores against it | done b1d785c — flex demand now follows the market (earliest ADP among the next-in-line at each eligible position), points only when nobody has an ADP. Live league: demand RB31/WR52 → **RB42/WR41**, baselines RB 157/WR 157 → **RB 118/WR 173** — the real best-available at pick 142 were 133 and 171. 2 tests | Gave 52 WR / 31 RB "startable", so RB replacement sat at RB31 ≈ 157 pts, equal to WR. The chat then told the user (picks 27–30) "it's not a positional lean — the baselines are nearly identical" and argued for the WR build. The room drafted **45 RBs by pick 132**, past the 31 the model assumed; the RB actually available then was 133 pts, not 157. Fix: derive flex share from what the room has drafted (live) or from ADP-implied start rates, not from the projections being scored. Blocks row 54. |
| 57 | Survival reads ADP only, never opponent need | todo | `draft::survival_probability(adp, now, at)`. A manager with zero RBs picking twice before your turn does not move an RB's odds. Depends on row 55 — the rosters it would read are wrong until then. |
| 58 | AI reasons from the raw VORP column, without the algorithm's discipline | partly done (prompt addendum in row 53), untested against a real session | "Rams DEF at 86" (round 7) and "Josh Downs — one flag: he's a Colt like Pierce" both came from the **chat**, not the recommender, which had DEF at −60 and (now) a same-team tilt. The user caught both by hand. Test the new prompt against `chats/1389710366300200961/1787963247363-6ac7.json` before trusting it. |
| 59 | Pre-read: top three options, N picks before yours, and again on the clock (asked in chat at pick 108) | todo | Auto-ask fires one "Who should I take next?" on the clock. The user asked for the three-option pre-read explicitly, then "start thinking now". |
| 60 | Board resent in full every question | todo | 27k context tokens and ~$0.46 per question; $21.44 over 47 questions tonight; the web-search question hit 90k tokens / $0.99. Send the board once per session and deltas after, or trim to the top N per position past round 8. |
| 61 | Persist `keeper_pick_nos` across refresh/relaunch | done b1d785c | Unflagged keepers (Achane 82, Gainwell 139, Stafford 195) lost their tag after a `refresh_data`. Now `keepers_{draft}.json` beside the manual picks: written whenever a keeper is first judged (load or poll), unioned back in on every load. 1 integration test through the stub (judged → draft passes the pick → forced reload → still a keeper) |
| 62 | "Won't last" sits below the fold at 1360 px with the chat open | todo | Offered at 17:30, no answer. It is the panel that most directly answers "what will be gone", and it was off-screen during the picks that mattered. |
| 63 | Web search end-to-end in the app | verify | Fixed at 17:33 (`--allowedTools`); four of the user's questions before that ("look up why", "try web search" on Zay Flowers) got "search is still blocked". Not yet confirmed working through the panel after the fix. |
| 65 | Projected standings: every team's best lineup and season points (asked 18:45) | done b1d785c | `lineup.rs`: greedy best lineup (dedicated slots, then narrowest flex first) and a week-by-week sum with byes honoured; `DraftView.projected_standings` best first; a **Projected standings** panel in the sidebar with the gap to the leader and your row marked; the chat is told about it. Final (draft complete, 210 picks, 40 trades): tbonsey 2301, fisher23 2065, ChrisWitz 2034, mbarish 2015 … McSleeper26 **9th at 1845 with an empty DEF slot** (no DEF drafted; a waiver unit is ~95 pts → ~6th). Five of tbonsey's nine starters came from other slots via trades — the old app credited them to Clemmy20/youngmomo. 4 Rust + 2 Vitest |
| 66 | Week 1 projections per team (asked 19:05) | done b1d785c | `LoadedLeague.weekly_points` (player → per-week points under league scoring, from the weekly rows already fetched for bonuses; `scoring::weekly_points_by_player`), `TeamProjection.{week, week_points, week_starters}` built from the week's own rows so byes/no-projection drop out, `lineup::UPCOMING_WEEK = 1` until the app tracks the calendar. Standings panel gains a Wk column and the week lineup in the hover. Final: tbonsey 150.2, mbarish 137.0, fisher23 136.2 … McSleeper26 10th at 119.3 (7th at 125.5 with the Giants DEF). 1 Rust + 1 Vitest |
| 67 | Shout about an empty required starter with picks running out | done b1d785c | The user finished with no DEF and three RBs at 167/170/198 that do not make the lineup; the recommender had DEF locked in for the last two rounds but the card is one line among many. `picks::starter_alert`: dedicated slots only (a flex is anyone's), fires when picks left ≤ holes + 1, `DraftStatus.starter_alert`; a red `role="alert"` bar above the clock: "DEF still empty with 1 pick left — fill it before a flier." 2 unit + 1 integration (through `build_view`) + 2 Vitest |
| 64 | Commit rows 50–53 | done 257f1fb · fc257bc · e2baaf9 | All applied, all green (`verify` exit 0 at 17:44 minus the LOC cap, now satisfied), nothing pushed. |

Things the draft *confirmed* working, for the record: the user's own pick
numbers, the snake strip, the 3 s poll (no missed picks, no false restarts
after 17:57), manual-pick reconciliation, session autosave/restore ($21 of chat
survived four app restarts), and the log — every finding above was reconstructed
from `draft-assistant.log` and the saved sessions rather than from memory.

## Season roadmap (researched 2026-08-28 ~19:30, after the draft)

Verified against the live API: `/state/nfl` (preseason week 3; opener in ~10 days),
`/league/{id}/matchups/{week}` (pairings + lineups as set; the user's DEF slot is `"0"`
and Gainwell is set over Shakir), `/league/{id}/transactions/{week}` (7 free-agent adds
within an hour of the draft ending; 38 trades and 517 moves last season), `/stats`
actuals, trending adds, and the `previous_league_id` chain. The API is read-only.
League: FAAB 1000, waivers Tuesday, trade deadline week 12, 6 playoff teams from
week 15, plus a league-average matchup every week.

| # | Item | Status | Notes |
|---|------|--------|-------|
| 68 | Current week from `/state/nfl` | done b1d785c | `NflState::upcoming_week` (opener until the regular season, then the live week); replaces the `UPCOMING_WEEK` constant |
| 69 | Lineup check vs the lineup set on Sleeper | done b1d785c | `matchup::lineup_check`: slot by slot, "X over Y, +gain", empty slots called out; same players in swapped slots is not a change. Sidebar **Week N** panel. Live: set 119.3 = best 119.3, **DEF reported empty** (nobody on the roster can fill it — the first version stayed silent on exactly that, caught by the live run). 6 unit + 3 integration + 3 Vitest |
| 70 | Matchup preview | done b1d785c | opponent from the shared `matchup_id`, their lineup as set (best if unset), margin, win probability from per-starter spread (CV 0.5 → ~20-pt team sigma). Week 1 live: vs MeatballMike09, 119.3–125.4, 42% to win |
| 71 | Waiver board with demand | done b1d785c | `waivers.rs`: gain = bye-adjusted season lineup total with the player minus without (exact, so a WR8 scores zero); `rivals_helped` = rivals he lifts by ≥15 (a point a week — the competition for the claim); Sleeper trending-add counts; at most 3 per position; drop candidates = players who never start. Live: Giants +92 (1 rival), Stroud +17 as QB2 for the Stafford bye, Helm +8 as TE2, every WR 0. `build_view` 127 ms. Bid suggester from last season's `waiver_bid` history deferred to row 75. 2 unit + 2 Vitest |
| 72 | Results ingestion + projected vs actual | done b1d785c | `results.rs`: standings from `/rosters` (record, points for/against), each past week's result from `/matchups` (opponent, score, W/L), and `trends` = per-player actual (matchups' `players_points`, league-scored) vs the week's projection, per game. Fetched only in the regular season (`engine_season.rs`, best-effort). Sidebar **Season through week N** panel. Verified on last season's league: 14 weeks, records match Sleeper exactly. Note `won` is head-to-head only; Sleeper's record also counts the league-average game. 3 unit + 1 Vitest |
| 73 | Transaction feed + trade finder | done b1d785c | `transactions.rs`: `/transactions/{week}` read as a timeline with names (trades with the picks that moved, claims with FAAB bids, adds/drops, commissioner moves; failed claims excluded). Trade finder: every one-for-one swap with every rival through the lineup engine both ways, kept only if both sides gain ≥3 and — the part that made it useful — **measured over the best free agent at that position**, since the first version proposed eight ways to trade a WR for a defense that is free on the wire. One idea per player, two per partner. Live: get Lamar Jackson for Wan'Dale Robinson from youngmomo (+33 over waiver, them +25); Texans DEF for the same (+17). `build_view` 345 ms. Sidebar **Trade ideas** + **League activity**. 3 unit + 1 Vitest. Deferred: a form to price an arbitrary offer; manager tendencies from last season |
| 74 | Playoff odds (Monte Carlo) | done b1d785c | `playoffs.rs`: the whole regular-season schedule from `/matchups` (Sleeper publishes it preseason; the engine now reads this week's matchups and the completed weeks off it instead of separate calls), `League.settings` (playoff_teams 6, playoff_week_start 15, league_average_match 1, trade deadline 12, FAAB 1000), records carried over from `/rosters`, weekly rows for every remaining week, per-starter spread, xorshift64* + Box–Muller (no crate), 2000 seeded seasons. Live: tbonsey 99.6%, ChrisWitz 91.5%, mbarish 88.6% … McSleeper26 **11.7%, 12.3 expected wins** (no DEF yet). **Playoffs** column in the standings panel. `build_view` 398 ms. 1 unit + Vitest |
| 75 | Season-mode polling + weekly briefs | done b1d785c | Once the draft is `complete` the poll loop drops to a 60 s beat and re-reads the season side (`AppCore::refresh_season` in `app_season.rs`: calendar, matchups, records, transactions, trending, schedule) every 30 min, replacing last time's season warnings rather than piling them up; the board/projections still come from **Refresh data** (6 h projection TTL). Chat gains **Waiver brief** and **Start/sit brief** buttons in season mode that send canned questions naming the state fields to use. 1 integration (stub hit counts) + 1 Vitest. Deferred: sending the board as a delta (cost), a daily injury refresh on its own timer |
| 76 | Last season: FAAB bid history + manager profiles, and the season fetch fanned out | done b1d785c | `history.rs` from the `previous_league_id` league (rosters, users, all 18 weeks of transactions; cached 7 days): trades, claims, winning-bid median/p75/max, per-manager trades/moves/FAAB/record. Live: 38 trades, 142 claims, 105 bids — median **$50**, p75 **$117**, max $586; rakmyteamup (left) 23 trades/86 moves, ocrevo 13/62, skoLinNH 8/38, Clemmy20 7/59, AllDay21 7/19. Waiver targets get a `suggested_bid` by rank (p75 / median / half), DEF and K capped at a token $1–10. Folded **Last season** panel. **Found while measuring: the season fetches ran in series and a load took 13.5 s** with one week timing out; `engine_season.rs` now fans out with `JoinSet` (client is `Clone`) → **1.8 s cold, zero warnings**, and a slow week only loses that week. 1 unit + 2 Vitest |
| 77 | Price an offer (trade evaluator) | done b1d785c | `trade.rs` (the finder and evaluator, split out of `transactions.rs` for the LOC cap): `evaluate(Offer{my_slot, partner_slot, give, get, week})` → both rosters before/after, season with byes and this week. `AppCore::evaluate_trade` + Tauri command `evaluate_trade`; `api.evaluateTrade` (preview explains it needs the desktop). Folded **Price an offer** form in the sidebar: partner select, tick what leaves and arrives, verdict with a one-line read ("They lose on it — expect a no"). Live through `AppCore`: Wan'Dale Robinson for Lamar Jackson with youngmomo → me +50, them +25, week 1 119.3 → 122.6 — the same numbers the finder gave. 1 unit + 1 integration + 2 Vitest |
| 78 | Chat cost: the board as a slice once the draft is over | done b1d785c | `prompt::board_for_prompt`: whole board while drafting; once `status == complete` the top 12 per position of the free-agent pool (the board arrives in value order). 419 rows → ~60, roughly 10k → 1.5k tokens of board per question; the state JSON (rosters, standings, waivers, ideas) is now the bulk. System prompt says which it is. 1 unit. Still deferred: sending only deltas of the state between turns |
| 79 | Injury-aware week lineups | done b1d785c | `Candidate`/`Starter` carry Sleeper's injury tag; `week_candidates` scores an Out/IR/PUP/Sus/Doubtful player at zero (kept on the list so a lineup that starts him is scored honestly), so the lineup check names the replacement: "Tee Higgins is Out — start Michael Wilson (11.6)". Questionable is not sidelined (through August it is half the league). Season and playoff math unchanged — a season projection already prices the games. 2 unit + Vitest |
| 80 | Bye-week map | done b1d785c | `lineup::bye_weeks`: every week with a bye on my roster, who is out, the best lineup that week (season points per game), the shortfall against a full week, and any starting slot nobody can fill; worst first. Live: **week 11 −31.8 (Bijan + Stafford out, QB slot empty)**, week 6 −15.4 (Higgins + LaPorta, TE empty), everything else under 4. Folded **Bye weeks** panel; the chat is told. 1 unit + 2 Vitest |
| 81 | Prompt state slimmed (measured) | done b1d785c | Measured on the real league before touching anything: the prompt was **79.8k chars (~20k tokens)** and the board only 3k of it — `projected_standings` carried every team's starters twice (31k) and `rosters` every player as a seven-field record (25k). Now standings keep their numbers and lose their lineups, and a rostered player is one line (`RB Jahmyr Gibbs DET`, `(keeper)` when he was). **31.5k chars (~7.9k tokens)**; the largest field is now `rosters` at 6k. System prompt updated to match. 1 unit |
| 82 | Daily injury refresh in season mode | done b1d785c | `refresh_season` (every 30 min once the draft is over) re-reads the 14 MB player dictionary when the cached one is a day old and applies it to the board in place (`board::apply_player_meta`: injury tags and teams, no rebuild); a failed fetch keeps yesterday's tags with a warning. Feeds the injury-aware lineup check (row 79). 1 unit + 1 integration (stub: new tag picked up once, not re-read while fresh) |
| 83 | Two-for-one trade ideas | done b1d785c | The finder now also tries a second piece from my *bench* with every one-for-one that the other side would refuse (their gain under 3), keeping the pair only when it is what makes them say yes and my gain over the wire still clears the bar; a one-for-one that already works is never padded. Their side is tested first and my side once per pair, so the pass costs ~290 ms on the real league (`build_view` 686 ms, from 1.18 s in the first cut). Live: Chris Olave for Wan'Dale Robinson + Michael Wilson from youngmomo (+73 / them +10); Nico Collins for Egbuka + Wan'Dale from Clemmy20 (+51 / +21); Tyler Warren (TE) for Pierce + Wan'Dale from ocrevo. Shown as "X for A + B". 1 unit (a scenario where singles are under the bar and the pair is over it) + Vitest |
